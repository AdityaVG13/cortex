pub(crate)fn scale_semantic_similarity(sim:f32)->f64{SEMANTIC_SCALE_BASE+(sim as f64-SEMANTIC_SIM_FLOOR)*((1.0-SEMANTIC_SCALE_BASE)/(1.0-SEMANTIC_SIM_FLOOR))}pub(crate)fn scale_semantic_similarity_with_keyword_overlap(sim:f32,text:&str,keyword_terms:&[String])->f64{let mut scaled=scale_semantic_similarity(sim);if!keyword_terms.is_empty(){let haystack=text.to_lowercase();let overlap=keyword_terms.iter().filter(|term|haystack.contains(term.as_str())).count();scaled*=if overlap==0{0.82}else{1.0+(overlap as f64/keyword_terms.len().max(1)as f64)*0.08};}scaled}fn upsert_best_semantic_candidate(candidates:&mut HashMap<String,SemanticCandidate>,source:String,excerpt:String,relevance:f64,importance:f64,ts:i64){let entry=candidates.entry(source.clone()).or_insert(SemanticCandidate{source,excerpt:excerpt.clone(),relevance,importance,ts});if relevance>entry.relevance{*entry=SemanticCandidate{source:entry.source.clone(),excerpt,relevance,importance,ts};}}#[derive(Clone,Copy)]enum SemanticEmbedKind{Memory,Decision}impl SemanticEmbedKind{fn target_type(self)->&'static str{match self{Self::Memory=>"memory",Self::Decision=>"decision"}}fn join_table(self)->&'static str{match self{Self::Memory=>"memories",Self::Decision=>"decisions"}}fn source_filter(self)->&'static str{match self{Self::Memory=>"(?3 IS NULL OR m.source LIKE ?3)",Self::Decision=>"(?3 IS NULL OR d.context LIKE ?3)",}}fn query_with_acl(self)->String{let table=self.join_table();let alias=if matches!(self,Self::Memory){"m"}else{"d"};let cols=match self{Self::Memory=>"e.vector, m.text, m.source, m.owner_id, m.visibility, m.score, m.trust_score, m.last_accessed, m.created_at",Self::Decision=>"e.vector, d.decision, d.context, d.owner_id, d.visibility, d.score, d.trust_score, d.last_accessed, d.created_at",};format!("SELECT {cols} FROM embeddings e JOIN {table} {alias} ON e.target_type = '{tt}' AND e.target_id = {alias}.id AND {alias}.status = 'active' AND ({alias}.expires_at IS NULL OR {alias}.expires_at > datetime('now')) AND ({alias}.valid_from IS NULL OR {alias}.valid_from <= datetime('now')) AND ({alias}.valid_until IS NULL OR {alias}.valid_until > datetime('now')) AND (e.model IS NULL OR LOWER(e.model) = ?1) AND (length(e.vector) = ?2 OR length(e.vector) = ?2/4 + 6) AND {}",self.source_filter(),tt=self.target_type())}fn query_without_acl(self)->String{let table=self.join_table();let alias=if matches!(self,Self::Memory){"m"}else{"d"};let cols=match self{Self::Memory=>"e.vector, m.text, m.source, NULL AS owner_id, NULL AS visibility, m.score, m.trust_score, m.last_accessed, m.created_at",Self::Decision=>"e.vector, d.decision, d.context, NULL AS owner_id, NULL AS visibility, d.score, d.trust_score, d.last_accessed, d.created_at",};format!("SELECT {cols} FROM embeddings e JOIN {table} {alias} ON e.target_type = '{tt}' AND e.target_id = {alias}.id AND {alias}.status = 'active' AND ({alias}.expires_at IS NULL OR {alias}.expires_at > datetime('now')) AND ({alias}.valid_from IS NULL OR {alias}.valid_from <= datetime('now')) AND ({alias}.valid_until IS NULL OR {alias}.valid_until > datetime('now')) AND (e.model IS NULL OR LOWER(e.model) = ?1) AND (length(e.vector) = ?2 OR length(e.vector) = ?2/4 + 6) AND {}",self.source_filter(),tt=self.target_type())}fn shadow_query_with_acl(self)->String{let table=self.join_table();let alias=if matches!(self,Self::Memory){"m"}else{"d"};let cols=match self{Self::Memory=>"e.vector, m.source, m.owner_id, m.visibility",Self::Decision=>"e.vector, d.decision, d.context, d.owner_id, d.visibility",};format!("SELECT {cols} FROM embeddings e JOIN {table} {alias} ON e.target_type = '{tt}' AND e.target_id = {alias}.id AND {alias}.status = 'active' AND ({alias}.expires_at IS NULL OR {alias}.expires_at > datetime('now')) AND ({alias}.valid_from IS NULL OR {alias}.valid_from <= datetime('now')) AND ({alias}.valid_until IS NULL OR {alias}.valid_until > datetime('now')) AND (e.model IS NULL OR LOWER(e.model) = ?1) AND (length(e.vector) = ?2 OR length(e.vector) = ?2/4 + 6) AND {}",self.source_filter(),tt=self.target_type())}fn shadow_query_without_acl(self)->String{let table=self.join_table();let alias=if matches!(self,Self::Memory){"m"}else{"d"};let cols=match self{Self::Memory=>"e.vector, m.source, NULL AS owner_id, NULL AS visibility",Self::Decision=>"e.vector, d.decision, d.context, NULL AS owner_id, NULL AS visibility",};format!("SELECT {cols} FROM embeddings e JOIN {table} {alias} ON e.target_type = '{tt}' AND e.target_id = {alias}.id AND {alias}.status = 'active' AND ({alias}.expires_at IS NULL OR {alias}.expires_at > datetime('now')) AND ({alias}.valid_from IS NULL OR {alias}.valid_from <= datetime('now')) AND ({alias}.valid_until IS NULL OR {alias}.valid_until > datetime('now')) AND (e.model IS NULL OR LOWER(e.model) = ?1) AND (length(e.vector) = ?2 OR length(e.vector) = ?2/4 + 6) AND {}",self.source_filter(),tt=self.target_type())}}fn prepare_acl_stmt<'a>(conn:&'a Connection,with_acl:&str,without_acl:&str)->Option<rusqlite::Statement<'a>>{match conn.prepare(with_acl){Ok(stmt)=>Some(stmt),Err(err)if is_missing_team_visibility_columns(&err)=>conn.prepare(without_acl).ok(),Err(_)=>None,}}fn decision_source_key(context:Option<String>,decision:&str)->String{context.unwrap_or_else(||format!("decision::{}",decision.chars().take(40).collect::<String>()))}pub(crate)fn collect_semantic_candidates(conn:&Connection,query_vector:&[f32],query_text:&str,ctx:&RecallContext,source_prefix:Option<&str>)->Vec<SemanticCandidate>{let selected_model=crate::embeddings::selected_model_key();let expected_vector_bytes=std::mem::size_of_val(query_vector)as i64;let source_like=source_prefix.map(|prefix|format!("{prefix}%"));let keyword_terms=extract_search_keywords(query_text);let semantic_floor=if keyword_terms.len()>=3{SEMANTIC_SIM_FLOOR+0.12}else{SEMANTIC_SIM_FLOOR};let mut candidates:HashMap<String,SemanticCandidate>=HashMap::new();for kind in[SemanticEmbedKind::Memory,SemanticEmbedKind::Decision]{let Some(mut stmt)=prepare_acl_stmt(conn,&kind.query_with_acl(),&kind.query_without_acl())else{continue};let Ok(rows)=stmt.query_map(params![selected_model,expected_vector_bytes,source_like.as_deref()],|row|{Ok((row.get::<_,Vec<u8>>(0)?,row.get::<_,String>(1)?,row.get::<_,Option<String>>(2)?,row.get::<_,Option<i64>>(3)?,row.get::<_,Option<String>>(4)?,row.get::<_,Option<f64>>(5)?,row.get::<_,Option<f64>>(6)?,row.get::<_,Option<String>>(7)?,row.get::<_,Option<String>>(8)?))})else{continue};for(blob,primary,alt,owner_id,visibility,score,trust_score,last_accessed,created_at)in rows.flatten(){if!is_visible(owner_id,visibility.as_deref(),ctx){continue;}let source=match kind{SemanticEmbedKind::Memory=>primary.clone(),SemanticEmbedKind::Decision=>decision_source_key(alt,&primary),};if!source_matches_prefix(&source,source_prefix){continue;}let sim=crate::embeddings::cosine_similarity(query_vector,&crate::embeddings::blob_to_vector(&blob));if sim<=semantic_floor as f32{continue;}let scaled=scale_semantic_similarity_with_keyword_overlap(sim,&primary,&keyword_terms);let excerpt=query_focused_excerpt(&primary,query_text,280);let importance=blend_importance(score,trust_score);let ts=parse_timestamp_ms(last_accessed.as_deref().or(created_at.as_deref()).unwrap_or_default());upsert_best_semantic_candidate(&mut candidates,source,excerpt,scaled,importance,ts);}}let mut sorted:Vec<SemanticCandidate>=candidates.into_values().collect();sorted.sort_by(|a,b|compare_relevance_desc_source_asc(a.relevance,&a.source,b.relevance,&b.source));sorted.truncate(MAX_SEMANTIC_RRF_CANDIDATES);sorted}pub(crate)fn collect_shadow_semantic_rows(conn:&Connection,ctx:&RecallContext,source_prefix:Option<&str>,expected_dimension:usize)->Vec<ShadowSemanticRow>{let selected_model=crate::embeddings::selected_model_key();let expected_vector_bytes=(expected_dimension*std::mem::size_of::<f32>())as i64;let source_like=source_prefix.map(|prefix|format!("{prefix}%"));let mut rows_by_source:HashMap<String,Vec<f32>>=HashMap::new();for kind in[SemanticEmbedKind::Memory,SemanticEmbedKind::Decision]{let Some(mut stmt)=prepare_acl_stmt(conn,&kind.shadow_query_with_acl(),&kind.shadow_query_without_acl())else{continue};let Ok(rows)=stmt.query_map(params![selected_model,expected_vector_bytes,source_like.as_deref()],|row|{Ok((row.get::<_,Vec<u8>>(0)?,row.get::<_,String>(1)?,row.get::<_,Option<String>>(2)?,row.get::<_,Option<i64>>(3)?,row.get::<_,Option<String>>(4)?))})else{continue};for(blob,primary,alt,owner_id,visibility)in rows.flatten(){if!is_visible(owner_id,visibility.as_deref(),ctx){continue;}let source=match kind{SemanticEmbedKind::Memory=>primary,SemanticEmbedKind::Decision=>decision_source_key(alt,&primary),};if!source_matches_prefix(&source,source_prefix){continue;}rows_by_source.entry(source).or_insert_with(||crate::embeddings::blob_to_vector(&blob));}}let mut rows:Vec<ShadowSemanticRow>=rows_by_source.into_iter().map(|(source,vector)|ShadowSemanticRow{source,vector}).collect();rows.sort_by(|a,b|a.source.cmp(&b.source));rows}pub(crate)fn vector_to_vec0_literal(vector:&[f32])->String{let mut literal=String::with_capacity(vector.len().saturating_mul(12).saturating_add(2));literal.push('[');for(idx,value)in vector.iter().enumerate(){if idx>0{literal.push_str(", ");}let _=write!(&mut literal,"{}",if value.is_finite(){*value}else{0.0});}literal.push(']');literal}pub(crate)fn run_sqlite_vec_shadow_knn_sources(conn:&Connection,query_vector:&[f32],candidates:&[ShadowSemanticRow],top_k:usize)->Result<Vec<String>,String>{if query_vector.is_empty()||candidates.is_empty(){return Ok(Vec::new());}const SHADOW_TABLE:&str="cortex_shadow_semantic_knn";let k=top_k.max(1).min(candidates.len());let query_literal=vector_to_vec0_literal(query_vector);let result=(||->Result<Vec<String>,String>{conn.execute_batch(&format!("DROP TABLE IF EXISTS {SHADOW_TABLE};")).map_err(|err|format!("sqlite-vec shadow drop failed: {err}"))?;conn.execute_batch(&format!("CREATE VIRTUAL TABLE {SHADOW_TABLE} USING vec0(candidate_id INTEGER PRIMARY KEY, embedding FLOAT[{}]);",query_vector.len())).map_err(|err|format!("sqlite-vec shadow create failed: {err}"))?;let insert_sql=format!("INSERT INTO {SHADOW_TABLE}(candidate_id, embedding) VALUES (?1, ?2)");let mut insert_stmt=conn.prepare(&insert_sql).map_err(|err|format!("sqlite-vec shadow insert prepare failed: {err}"))?;for(candidate_idx,candidate)in candidates.iter().enumerate(){let candidate_id=i64::try_from(candidate_idx+1).map_err(|_|"sqlite-vec shadow candidate id overflow".to_string())?;insert_stmt.execute(params![candidate_id,vector_to_vec0_literal(&candidate.vector)]).map_err(|err|format!("sqlite-vec shadow insert failed: {err}"))?;}let k_i64=i64::try_from(k).map_err(|_|"sqlite-vec shadow k overflow".to_string())?;let query_sql=format!("SELECT candidate_id, distance FROM {SHADOW_TABLE} WHERE embedding MATCH ?1 AND k = ?2");let mut query_stmt=conn.prepare(&query_sql).map_err(|err|format!("sqlite-vec shadow query prepare failed: {err}"))?;let rows=query_stmt.query_map(params![query_literal,k_i64],|row|Ok((row.get::<_,i64>(0)?,row.get::<_,f64>(1)?))).map_err(|err|format!("sqlite-vec shadow query failed: {err}"))?;let mut sources=Vec::new();let mut seen=HashSet::new();for row in rows{let(candidate_id,_distance)=row.map_err(|err|format!("sqlite-vec shadow row decode failed: {err}"))?;if candidate_id<=0{continue;}let Some(candidate)=candidates.get((candidate_id-1)as usize)else{continue};if seen.insert(candidate.source.clone()){sources.push(candidate.source.clone());}}Ok(sources)})();let _=conn.execute_batch(&format!("DROP TABLE IF EXISTS {SHADOW_TABLE};"));result}pub(crate)fn shadow_error_to_unavailable_reason(error:&str)->Option<&'static str>{if error.to_ascii_lowercase().contains("no such module: vec0"){Some("sqlite_vec_not_available")}else{None}}pub(crate)fn build_shadow_semantic_explain(conn:&Connection,query_vector:Option<&[f32]>,query_text:&str,ctx:&RecallContext,source_prefix:Option<&str>,top_k:usize,baseline_override:Option<&ShadowSemanticBaseline>)->Value{let top_k=top_k.clamp(1,MAX_SEMANTIC_RRF_CANDIDATES);let Some(query_vector)=query_vector else{return json!({"enabled":true,"status":"unavailable","reason":"query_embedding_unavailable","topK":top_k});};if query_vector.is_empty(){return json!({"enabled":true,"status":"unavailable","reason":"query_embedding_empty","topK":top_k});}let(baseline_candidate_count,baseline_top_sources)=if let Some(baseline)=baseline_override{(baseline.candidate_count,baseline.top_sources(top_k))}else{let baseline=collect_semantic_candidates(conn,query_vector,query_text,ctx,source_prefix);let top_sources=baseline.iter().take(top_k).map(|candidate|candidate.source.clone()).collect();(baseline.len(),top_sources)};let rows=collect_shadow_semantic_rows(conn,ctx,source_prefix,query_vector.len());if rows.is_empty(){return json!({"enabled":true,"status":"unavailable","reason":"no_shadow_candidates","topK":top_k,"baselineCandidateCount":baseline_candidate_count,"baselineTopSources":baseline_top_sources});}let vector_dim=query_vector.len();let compatible_rows:Vec<ShadowSemanticRow>=rows.into_iter().filter(|row|row.vector.len()==vector_dim).collect();if compatible_rows.is_empty(){return json!({"enabled":true,"status":"unavailable","reason":"no_dimension_compatible_candidates","topK":top_k,"vectorDimension":vector_dim,"baselineCandidateCount":baseline_candidate_count,"baselineTopSources":baseline_top_sources});}let compatible_count=compatible_rows.len();let shadow_top_sources=match run_sqlite_vec_shadow_knn_sources(conn,query_vector,&compatible_rows,top_k){Ok(sources)=>sources,Err(error)=>{if let Some(reason)=shadow_error_to_unavailable_reason(&error){return json!({"enabled":true,"status":"unavailable","reason":reason,"detail":error,"topK":top_k,"vectorDimension":vector_dim,"baselineCandidateCount":baseline_candidate_count,"shadowCandidateCount":compatible_count,"baselineTopSources":baseline_top_sources});}return json!({"enabled":true,"status":"error","reason":error,"topK":top_k,"vectorDimension":vector_dim,"baselineCandidateCount":baseline_candidate_count,"shadowCandidateCount":compatible_count,"baselineTopSources":baseline_top_sources});}};let baseline_set:HashSet<&str>=baseline_top_sources.iter().map(String::as_str).collect();let shadow_set:HashSet<&str>=shadow_top_sources.iter().map(String::as_str).collect();let overlap_count=baseline_set.intersection(&shadow_set).count();let union_count=baseline_set.union(&shadow_set).count();let overlap_ratio=if top_k==0{0.0}else{round4(overlap_count as f64/top_k as f64)};let jaccard=if union_count==0{1.0}else{round4(overlap_count as f64/union_count as f64)};let baseline_index:HashMap<&str,usize>=baseline_top_sources.iter().enumerate().map(|(idx,source)|(source.as_str(),idx)).collect();let shadow_index:HashMap<&str,usize>=shadow_top_sources.iter().enumerate().map(|(idx,source)|(source.as_str(),idx)).collect();let mut matched_rank_pairs:usize=0;let mut rank_delta_sum:usize=0;for(source,baseline_rank)in&baseline_index{if let Some(shadow_rank)=shadow_index.get(source){matched_rank_pairs+=1;rank_delta_sum+=baseline_rank.abs_diff(*shadow_rank);}}let mean_abs_rank_delta=if matched_rank_pairs>0{Some(round4(rank_delta_sum as f64/matched_rank_pairs as f64))}else{None};let top1_match=match(baseline_top_sources.first().map(String::as_str),shadow_top_sources.first().map(String::as_str)){(Some(left),Some(right))=>Some(left==right),_=>None,};json!({"enabled":true,"status":"ok","topK":top_k,"vectorDimension":vector_dim,"baselineCandidateCount":baseline_candidate_count,"shadowCandidateCount":compatible_count,"baselineTopSources":baseline_top_sources,"shadowTopSources":shadow_top_sources,"overlapCount":overlap_count,"overlapRatio":overlap_ratio,"jaccard":jaccard,"matchedRankPairs":matched_rank_pairs,"meanAbsRankDelta":mean_abs_rank_delta,"top1Match":top1_match,})}
