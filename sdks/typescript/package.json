{
  "name": "@cortex-memory/client",
  "version": "0.6.0",
  "description": "TypeScript SDK for the Cortex AI Memory daemon",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "npm run build && node --test ./test/*.test.mjs"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "25.9.1",
    "typescript": "6.0.3"
  }
}
