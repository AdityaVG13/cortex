# Comprehensive Changelog (Living)

Last updated: 2026-04-20
Baseline: v0.4.1 (2026-04-06)
Current head: 8629c4f (2026-04-20)
Commit range size: 396 commits
## Purpose
This file tracks all meaningful changes since v0.4.1 for release preparation and auditability.
Keep this file additive. Do not delete history; append updates as Cortex evolves.

- 2026-04-20 19:14 - MCP tools/call hot-path allocation reduction (`8629c4f`):
  - `daemon-rs/src/handlers/mcp.rs`:
    - `tools/call` known-tool validation now uses direct match-based permission lookup.
    - removed per-call `mcp_tools()` vector construction/scan from request hot path.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::mcp::tests:: -- --nocapture` (`21` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml tools_call_includes_token_usage_line_for_cortex_tools -- --nocapture` (`1` passing)

- 2026-04-20 19:13 - headlines/peek token-accounting accuracy fix (`1b158d8`):
  - `daemon-rs/src/handlers/recall.rs`:
    - headlines mode (`budget=0`) now computes real token usage from returned source headlines instead of fixed zeros.
    - headlines mode now reports estimated savings versus full-excerpt recall output.
    - `/peek` now uses shared headlines token-usage accounting for consistent spent/saved semantics.
    - updated `format_recall_token_usage_line` headlines branch to emit explicit savings text when available.
    - added regression `execute_unified_recall_headlines_reports_token_usage_and_savings`.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml execute_unified_recall_headlines_reports_token_usage_and_savings -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests:: -- --nocapture` (`120` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::mcp::tests:: -- --nocapture` (`21` passing)
    - `npm test` in `desktop/cortex-control-center` (`89` passing)

- 2026-04-20 19:10 - token usage/savings visibility across Cortex function surfaces (`5978c81`):
  - `daemon-rs/src/handlers/mcp.rs`:
    - added generic payload decoration so Cortex `tools/call` responses now consistently include:
      - `tokenUsage` (`used`, `saved`, `budget`)
      - `tokenUsageLine`
    - boot payload now explicitly includes `tokenUsage` and budget-aware usage line.
    - added regression `tools_call_includes_token_usage_line_for_cortex_tools` spanning multiple read/write/admin tool paths.
  - `daemon-rs/src/handlers/boot.rs`:
    - HTTP `/boot` now returns `tokenUsage` + `tokenUsageLine` with used/saved/budget visibility.
  - `daemon-rs/src/handlers/recall.rs`:
    - `/peek` now returns token usage/savings visibility versus full-excerpt recall output.
    - unified/shared budget accounting helpers now enforce deterministic budget invariants and usage-line output across recall surfaces.
    - added deterministic regressions for low-signal term filtering, study-abroad intent alias recovery, and budget-invariant trimming.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml tools_call_includes_token_usage_line_for_cortex_tools -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::mcp::tests:: -- --nocapture` (`21` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests:: -- --nocapture` (`119` passing)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml --release` (pass)
    - `npm test` in `desktop/cortex-control-center` (`89` passing)

- 2026-04-20 18:36 - route-class rate-limit isolation for recall/store (`0a59e60`):
  - `daemon-rs/src/rate_limit.rs`:
    - request buckets are now keyed by `(ip, request_class)` with dedicated classes: `default`, `recall`, `store`.
    - added optional class-specific env limits:
      - `CORTEX_RATE_LIMIT_RECALL_REQUESTS_PER_MIN`
      - `CORTEX_RATE_LIMIT_RECALL_LOOPBACK_REQUESTS_PER_MIN`
      - `CORTEX_RATE_LIMIT_STORE_REQUESTS_PER_MIN`
      - `CORTEX_RATE_LIMIT_STORE_LOOPBACK_REQUESTS_PER_MIN`
    - added regression `test_route_class_buckets_are_independent`.
  - `daemon-rs/src/handlers/mod.rs`:
    - auth-rate helpers now support class-aware enforcement (`ensure_auth_rated_for_class`, `ensure_auth_with_caller_rated_for_class`) with existing wrappers preserved.
  - `daemon-rs/src/handlers/store.rs` + `daemon-rs/src/handlers/recall.rs`:
    - `/store` requests now consume the store class bucket.
    - recall-family endpoints now consume the recall class bucket.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml rate_limit::tests::` (`7` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::store::tests:: -- --nocapture` (`21` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`116` passing)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml --release` (pass)

- 2026-04-20 18:33 - recall budget packing early-stop once intent coverage is met (`5cafbd7`):
  - `daemon-rs/src/handlers/recall.rs`:
    - budget candidate selection now stops early when:
      - at least two items are selected,
      - query-term coverage is complete,
      - and budget pressure crosses threshold (`0.82`).
    - prevents low-marginal tail snippets from consuming token budget after intent coverage is already satisfied.
    - added regression `should_early_stop_budget_selection_requires_coverage_and_pressure`.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`116` passing)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml --release` (pass)

- 2026-04-20 18:29 - adaptive default recall budget by query shape (`5b4a2f4`):
  - `daemon-rs/src/handlers/recall.rs`:
    - default `/recall` and `/recall/explain` calls (no explicit mode/budget) now apply query-shape + `k` adaptive budget caps.
    - short/exact identifier-like queries now default to lower budgets; broad natural-language queries retain higher headroom.
    - explicit operator settings (`budget`/`policyMode`) remain authoritative and are not overridden.
    - added regressions:
      - `maybe_apply_adaptive_default_budget_reduces_short_exact_queries`
      - `maybe_apply_adaptive_default_budget_preserves_long_natural_query_headroom`
      - `maybe_apply_adaptive_default_budget_does_not_override_explicit_settings`
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`115` passing at commit time)

- 2026-04-20 18:26 - benchmark base-adapter personal recall determinism (`e4fcab3`):
  - `benchmarking/adapters/cortex_http_base_provider.py`:
    - expanded personal-memory query intent handling (name/surname change, repaint/color item details, location qualifiers).
    - removed benchmark-budget escalation for detail queries; adapter now respects configured budget (`300`) consistently.
    - improved detail reranking bonuses for concrete `[user-answer]` snippets over generic answers.
  - `benchmarking/adapters/tests/test_cortex_http_base_provider.py`:
    - added deterministic regressions for name-detail variants, item-detail variants, location qualifier choices, and generic-vs-concrete item answers.
  - Validation:
    - `pytest benchmarking/adapters/tests/test_cortex_http_base_provider.py -q` (`17` passing)

- 2026-04-20 18:24 - recall redundancy guard + answer-span prioritization (`99a8d27`):
  - `daemon-rs/src/handlers/recall.rs`:
    - budget packing now drops near-duplicate candidates when they do not add new query-term coverage.
    - query-focused excerpt extraction now prefers `[user-answer]` spans for QA-style transcript memories.
    - switched focused-excerpt lowercase path to ASCII-lowercase for stable byte-index alignment in clipping.
    - added regressions:
      - `test_query_focused_excerpt_prefers_user_answer_block_for_qa_memory`
      - `test_apply_semantic_budget_skips_redundant_candidates_without_new_coverage`
      - `test_apply_semantic_budget_keeps_similar_candidate_with_new_query_term_coverage`
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`112` passing at commit time)

- 2026-04-20 18:11 - local codex cache ignore hygiene (`7ffc86c`):
  - `.gitignore`:
    - added `.codex-cache/` to keep operator-tooling cache directories out of normal repo status flow.

- 2026-04-20 18:09 - sqlite runtime tuning controls (`3bdd9b1`):
  - `daemon-rs/src/db.rs`:
    - sqlite configure path now supports bounded env-driven tuning for:
      - `CORTEX_DB_MMAP_SIZE_BYTES`
      - `CORTEX_DB_CACHE_SIZE_KIB`
    - enabled `PRAGMA temp_store = MEMORY` for temp-table heavy query flows.
    - defaults adjusted for higher-throughput local daemon workloads while retaining safe clamps.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml configure -- --nocapture` (`4` passing)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml` (pass)

- 2026-04-20 18:08 - release footprint tuning (`9516892`):
  - `daemon-rs/Cargo.toml`:
    - release profile now sets `panic = "abort"` to reduce binary footprint and unwind overhead in release daemon builds.
  - Validation:
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml --release` (pass)

- 2026-04-20 18:07 - recall query-text processing hot-path reduction (`581ee46`, `c38cf3e`, `154c884`):
  - `daemon-rs/src/handlers/recall.rs`:
    - query-alignment profile now reused in ranking/fallback paths instead of repeated term extraction per comparator call.
    - fallback ranking now carries precomputed alignment metadata and uses it directly in stable sort tie-breaks.
    - excerpt focus terms are precomputed once per query in memory/decision search paths and reused for focused excerpt extraction.
    - family candidate compaction now reuses query-alignment profile when choosing the preferred representative.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`109` passing)

- 2026-04-20 17:51 - browser-harness scope correction (`a464251`):
  - removed `desktop/cortex-control-center/scripts/runtime-perf-check-harness.py` from repo.
  - browser-harness remains internal operator tooling and is not shipped as Cortex product/repo functionality.

- 2026-04-20 17:50 - runtime artifact hygiene (`e78aa91`):
  - `.gitignore`:
    - added ignores for desktop runtime verification artifacts and local dev pid markers:
      - `desktop/cortex-control-center/runtime-artifacts/`
      - `desktop/cortex-control-center/.codex-dev-baseline.pid`
      - `desktop/cortex-control-center/.devrun-live.pid`

- 2026-04-20 17:49 - browser-harness real-runtime probe (`c79e073`):
  - `desktop/cortex-control-center/scripts/runtime-perf-check-harness.py`:
    - added browser-harness-based runtime probe for real app UX/perf capture without Playwright (later removed from repo in `a464251` per tooling-scope correction).
    - captures panel switch timings, web-vitals snapshot, `/peek` + `/recall` smoke checks, and full-page screenshots.
    - enforces singleton Control Center tab lifecycle for repeated runs (closes previous app tabs before relaunch).
    - adds conflict-surface screenshot capture by scrolling Memory panel to the Conflict Resolution section.
    - tags recall smoke requests with unique source-agent scope for cleaner repeat-run behavior.
  - Validation:
    - baseline capture: `runtime-artifacts/baseline-harness-2026-04-20T17-30-00`
    - final capture: `runtime-artifacts/after-harness-final-r2-2026-04-20T17-49-35`

- 2026-04-20 17:44 - recall ranking + dedup hot-path hardening (`4223be2`):
  - `daemon-rs/src/handlers/recall.rs`:
    - added temporal intent detection and recency multiplier for "latest/current" style queries.
    - added query-alignment relevance boost based on exact phrase + focus-term coverage.
    - improved deterministic comparator handling by demoting non-finite relevance values.
    - optimized served-content dedup path to snapshot and stage hash writes, minimizing repeated async lock re-entry.
    - expanded synonym alias coverage for practical personal-memory phrasing.
    - added/updated deterministic recall smoke and recency-alignment regressions.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests::` (`109` passing)

- 2026-04-20 17:43 - desktop runtime/UI performance and UX polish (`735deb9`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - Brain panel mount policy changed to keep panel mounted after first visit (`brainPanelMounted`) to avoid remount churn.
    - memory `/peek` and `/recall` UI actions now use auth-required API path.
    - analytics throughput now uses dedicated summary helper with deterministic calendar-window semantics.
    - Connection Settings dialog now has explicit close `x` affordance.
  - `desktop/cortex-control-center/src/BrainVisualizer.jsx`:
    - hidden Brain view now pauses animation + pointer interaction; active view resumes.
    - component wrapped in `memo(...)` to reduce rerender pressure from parent updates.
  - `desktop/cortex-control-center/src/analytics-metrics.js` + `.test.js`:
    - added throughput summarization utility and coverage for zero-fill/partial-history behavior.
  - `desktop/cortex-control-center/src/styles.css`:
    - added styles for Connection Settings close control.
  - Validation:
    - `npm test` in `desktop/cortex-control-center` (`89` passing)
    - runtime harness aggregate tab-switch average improved (`182.15ms` -> `167.84ms`, same machine/session)

- 2026-04-19 23:36 - compiled-context window + budget clarity (`4a51aef`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - renamed `Compiled context` metric label to `Boot Prompt Tokens (30d total)` so operators do not mistake the value for per-boot budget.
    - replaced footnote with explicit per-boot budget signal (`avgServedPerBoot`) for direct comparison against target prompt-size goals.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`85` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)

- 2026-04-19 23:33 - throughput metric window consistency fix (`52fc17a`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - Throughput card now reports a deterministic rolling 7-day boot count derived from `savings.daily` instead of ambiguous `summary.totalBoots`.
    - updated label/footnote to make metric scope explicit: `7d Boot Compilations` from local boot-savings events (benchmarks excluded).
    - Boots Per Day card badge now explicitly shows `last 30d` total derived from the daily series, clarifying the longer-window aggregate.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`85` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)

- 2026-04-19 23:29 - analytics card metric-semantics correction (`11703b1`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - replaced Economic value footnote copy that referenced recall hit rate.
    - Economic value card now keeps savings-specific explanatory text; recall hit-rate messaging remains in the dedicated Recall Quality card.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`85` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)

- 2026-04-19 23:22 - analytics economic-value assumptions legend (`744bcd4`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - added `savingsEstimateLegend` to make the Economic value estimate basis explicit in-card (`$15 USD per 1M tokens saved`), including conversion notice for non-USD currency display.
  - `desktop/cortex-control-center/src/styles.css`:
    - added `.metric-assumption` styling for a compact inline assumption chip in the Economic value metric card.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`85` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)

- 2026-04-19 23:11 - deterministic recall tie-breaks + temporal guard hardening (`04c9e2e`):
  - `daemon-rs/src/handlers/recall.rs`:
    - introduced deterministic relevance comparator (`compare_relevance_desc_source_asc`) and applied it to unified/semantic/budget candidate pools to avoid hash-map iteration nondeterminism when scores tie.
    - strengthened keyword/decision ranking tie resolution with query-alignment tie-break before lexical source fallback so text-evidence wins over metadata-only matches under equal score conditions.
    - made weighted RRF output deterministic with stable `id` tie-break.
    - added regression `test_search_memories_fallback_breaks_ties_by_source`.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::recall::tests -- --nocapture` (`102` passed)

- 2026-04-19 23:02 - recall temporal validity filtering (Step 9.2 tranche) (`8cffed5`):
  - `daemon-rs/src/handlers/recall.rs`:
    - recall query paths now enforce temporal validity windows in addition to expiry checks:
      - `(valid_from IS NULL OR valid_from <= now)`
      - `(valid_until IS NULL OR valid_until > now)`
    - temporal predicates were applied across keyword search, semantic candidate collection, shadow-semantic row collection, associative source lookup, and unfold source hydration queries.
    - expanded regressions:
      - `search_memories_excludes_temporally_invalid_rows`
      - `search_decisions_excludes_temporally_invalid_rows`
      - `semantic_candidate_collection_excludes_temporally_invalid_rows`
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml temporally_invalid -- --nocapture` (`3` passed)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::recall::tests -- --nocapture` (`101` passed)

- 2026-04-19 22:53 - Mission Control persistent unit key legend (`55c13c3`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - replaced ambiguous `Metrics ?` control text with `Metric Legend`.
    - added always-visible unit key chips in Mission Control (`t`, `K`, `M`, `B`, `T`, `t/day`) so compact values are interpretable without opening a dialog.
    - clarified case-sensitive suffix semantics in expanded legend (`t` tokens, `T` trillions).
  - `desktop/cortex-control-center/src/styles.css`:
    - added styling for persistent unit-key strip/chips with mobile-safe wrapping.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`85` passing)

- 2026-04-19 22:51 - temporal semantics import/export upgrade (Step 9.2 tranche) (`833fb8d`):
  - `daemon-rs/src/db.rs`:
    - schema migration `014` adds temporal semantics columns (`observed_at`, `valid_from`, `valid_until`) to both `memories` and `decisions`.
    - fresh schema bootstrap now includes the same temporal columns, and migration regression assertions were expanded.
  - `daemon-rs/src/api_types.rs`:
    - import DTOs now accept optional temporal fields for memories/decisions.
  - `daemon-rs/src/export_data.rs`:
    - JSON full/changeset exports now emit temporal fields.
    - import path now normalizes memory/decision types and persists temporal fields for both owner-aware and legacy schema branches.
    - SQL export now preserves temporal fields for memories/decisions.
    - added regression `import_payload_normalizes_types_and_preserves_temporal_fields`.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml export_data::tests -- --nocapture` (`3` passed)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`424` passed)

- 2026-04-19 22:37 - feature intake guardrail validator (Step 9.5 tranche) (`6a61989`):
  - added `tools/validate_feature_intake.py` to enforce per-feature expected-gain/resource-cost/rollback/measurement fields and duplicate-id checks.
  - added `tools/tests/test_validate_feature_intake.py` (`4` passing).
  - wired CI gate in `.github/workflows/ci.yml` (`spawn-audit` job).
  - seeded `docs/internal/feature-intake/manifest.json` with completed tranche entries.
  - Validation:
    - `rtk python tools/validate_feature_intake.py` (pass)
    - `rtk python -m pytest tools/tests/test_validate_feature_intake.py -q` (`4` passed)

- 2026-04-19 22:33 - eval metrics + regression gate expansion (Step 9.4 tranche) (`fbff1f5`):
  - `daemon-rs/src/eval.rs`:
    - expanded eval snapshot with task-family metrics (`taskSuccessRate`, `firstPassSuccess`, `medianTimeToValidResultMs`, `retryCount`) and memory quality metrics (`staleMemoryHitRate`, `lowTrustHitRate`, `consensusPromotionPrecision`) plus baseline/assisted/delta block.
    - added `build_eval_regression_gate(...)` for fail-closed rate regression checks.
  - `daemon-rs/src/main.rs`:
    - `cortex eval` now supports `--baseline-file`, `--max-regression`, and `--fail-on-regression`.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`423` passed at tranche validation time)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-19 22:21 - recall policy modes + latency fail-closed guard (Step 9.5 tranche) (`85d3ede`):
  - `daemon-rs/src/handlers/recall.rs`:
    - added policy modes (`headlines`, `fast`, `balanced`, `deep`) with mode-aware budget/k resolution and explicit parser coverage.
    - added hard per-mode latency budgets with fail-closed fallback to deterministic keyword path.
    - unified recall telemetry now includes `policyMode`/`requestedPolicyMode` and fail-closed indicators.
  - `daemon-rs/src/handlers/mcp.rs`:
    - MCP recall tools now accept `policyMode` and return resolved policy metadata.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`422` passed at tranche validation time)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-20 01:40 - local eval snapshot harness (Step 9.4 tranche) (`5be5310`):
  - `daemon-rs/src/eval.rs`:
    - added shared local eval snapshot builder for windowed reliability/memory-quality signals.
    - emits totals/window/signals payload including `conflictBurden`, `decayBurden`, `resolutionVelocity`, and `contradictionRate`.
    - added dedicated regression `eval_snapshot_computes_expected_signals`.
  - `daemon-rs/src/handlers/mcp.rs`:
    - `cortex_eval_run` now reuses shared eval snapshot logic instead of duplicating SQL signal calculations inline.
  - `daemon-rs/src/main.rs`:
    - added `cortex eval [--window-days <n>] [--json]` operator CLI for local eval snapshots without going through MCP.
    - updated CLI help text accordingly.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`419` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-20 00:35 - daemon sync primitives (Step 7 closeout tranche) (`1a03003`):
  - `daemon-rs/src/main.rs`:
    - added first-class `cortex sync` CLI with `export`, `import`, and `watch` subcommands.
    - `sync export` now supports incremental cursor flow via `--since` and `--cursor-file`.
    - `sync watch` now supports transport-agnostic watched-folder flows with local seen-state tracking under `~/.cortex/runtime/sync-watch` to avoid cross-device marker pollution.
    - import path refactored into shared helpers (`parse_import_cli_args`, `import_payload_from_file`, `open_cli_connection`) so `import` and `sync import/watch` use the same validation and team-owner semantics.
  - `daemon-rs/src/export_data.rs`:
    - added deterministic changeset export payload (`mode=changeset`) and bounded sync windowing (`since` lower bound + `cursor` upper bound) to prevent replay races.
    - added new changeset coverage for since-cutoff filtering and cursor upper-bound behavior.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`418` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-20 01:10 - activation/idle economics hardening (Step 8 implementation tranche) (`656e9ad`):
  - `daemon-rs/src/server.rs`:
    - added request-activity middleware to update runtime activity timestamps on all HTTP routes.
    - added Unix socket-activation adoption via `LISTEN_FDS`/`LISTEN_PID` with safe fallback to normal bind paths and explicit listener diagnostics.
    - `run_plain`/`run_tls` now accept optional pre-activated listeners, preserving existing TLS/non-TLS startup logic.
  - `daemon-rs/src/state.rs`:
    - added runtime activity tracking primitives (`last_activity_unix_secs`, `mark_activity_now`, `idle_for_secs`) initialized at daemon startup.
  - `daemon-rs/src/main.rs`:
    - added optional idle-shutdown orchestration:
      - `CORTEX_IDLE_SHUTDOWN_SECS`
      - `CORTEX_IDLE_SHUTDOWN_MIN_UPTIME_SECS`
    - shutdown future now cleanly exits when idle threshold is reached while preserving existing HTTP/extra shutdown signals.
  - `daemon-rs/src/handlers/{admin,mcp,mutate,recall,store}.rs`:
    - updated test-state constructors to include runtime activity field.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`418` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-19 23:40 - Mission Control units legend + compact/full number toggle (`b7a225b`):
  - `desktop/cortex-control-center/src/App.jsx`:
    - added Mission Control metric legend overlay with explicit unit semantics.
    - added compact/full number toggle for token metrics (`t`, `t/day` vs full `tokens`, `tokens/day`) in the Mission Control card.
  - `desktop/cortex-control-center/src/styles.css`:
    - added styling for Mission Control legend and control affordances.
  - Validation:
    - `rtk npm test` in `desktop/cortex-control-center` (`85` passing)
    - `rtk npm run web:build` in `desktop/cortex-control-center` (pass)

- 2026-04-19 20:50 - MCP intelligence + maintenance APIs (`639bc0a`):
  - `daemon-rs/src/handlers/mcp.rs`:
    - added new MCP tools: `cortex_consensus_promote`, `cortex_memory_decay_run`, and `cortex_eval_run`.
    - added explicit admin permission gating for all three tools in `required_permission_for_tool(...)`.
    - implemented `cortex_consensus_promote` with trust-based winner selection (`trustScore`/`confidence`), `minMargin` thresholding, `dryRun` support, and durable conflict-resolution writes.
    - implemented `cortex_memory_decay_run` to execute one explicit score-decay pass plus optional aging and expired-row cleanup.
    - implemented `cortex_eval_run` to emit a windowed evaluation snapshot for conflict pressure, decay burden, and resolution velocity.
    - added regression tests for permission enforcement and behavioral execution paths.
  - Validation:
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-tests handlers::mcp::tests -- --nocapture` (`20` passed)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-tests --test mcp_transport -- --nocapture` (`11` passed)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-tests` (pass)

- 2026-04-19 20:20 - derived-state repair CLI expansion (`c35603a`):
  - `daemon-rs/src/main.rs`:
    - added `cortex reindex [--json]` to fully rebuild FTS indexes from canonical memory/decision rows.
    - added `cortex re-embed` / `cortex reembed` aliases that force `embeddings drain --until-exhausted`.
    - added `cortex recrystallize [--json]` to clear crystal rows/embeddings and rebuild crystallization output in one operator command.
    - updated CLI help text to expose all three repair commands.
  - `daemon-rs/src/db.rs`:
    - added `reindex_fts(...)` using FTS-native `delete-all` before repopulating from canonical tables, ensuring stale/orphan FTS rows are removed instead of only filling missing rows.
    - added regression test `test_reindex_fts_removes_orphan_rows_and_rebuilds_from_base_tables`.
  - Validation:
    - `npm test` in `desktop/cortex-control-center` (`85` passing)
    - `cargo test --manifest-path daemon-rs/Cargo.toml reindex_fts -- --nocapture` with isolated target dir (`1` passing)
    - `cargo test --manifest-path daemon-rs/Cargo.toml --test mcp_transport -- --nocapture` with isolated target dir (`11` passing)

- 2026-04-19 20:45 - startup recovery + retrieval defaults + matrix cadence closeout (local uncommitted batch):
  - `desktop/cortex-control-center/src/daemon-startup.js`, `src/daemon-startup.test.js`, `src/App.jsx`:
    - added explicit transient-feedback classification for startup/warmup/timed-out daemon notices so stale startup banners are automatically cleared once healthy state resumes.
    - closes the long-lived "Daemon is still starting. Reconnect will continue automatically." banner persistence path.
  - `desktop/cortex-control-center/scripts/ensure-daemon-dev-binary.mjs`:
    - Windows stale-daemon cleanup subprocess now uses piped stdio instead of inherited console attachment to avoid ghost terminal flashes during dev boot.
  - `desktop/cortex-control-center/src/analytics-projection.js`, `src/analytics-projection.test.js`:
    - added hard absolute daily/projected-gain caps to Monte Carlo projection shaping, preventing extreme historical outliers from producing unreadable mission-control gain values.
  - `daemon-rs/src/state.rs`:
    - sqlite-vec route default now resolves to `primary` (invalid/missing route env values now also fall back to `primary`), retaining fail-closed baseline fallback in guarded routing logic.
  - `daemon-rs/src/embeddings.rs`:
    - embedding model default upgraded to `all-MiniLM-L12-v2`; legacy L6 remains selectable via explicit aliases.
    - unknown model values now fail safe to modern default.
  - `daemon-rs/src/handlers/recall.rs`:
    - second-pass BM25 default rebalance applied (stronger text emphasis, reduced source/context dominance) while retaining runtime override knobs and clamp bounds.
  - `benchmarking/run_amb_cortex.py`, `benchmarking/adapters/tests/test_run_amb_cortex_matrix.py`:
    - added `cadence` command to run an ordered matrix-file sequence (with per-matrix summaries and continue-on-error support), providing a concrete broader rerun cadence beyond the strict LongMemEval gate path.
    - added cadence dry-run and continue-on-error regression coverage.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center run test -- src/daemon-startup.test.js src/analytics-projection.test.js src/number-format.test.js` (`18` passing)
    - `rtk pytest benchmarking/adapters/tests/test_run_amb_cortex_matrix.py -vv` (`28` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex sqlite_vec_canary_config_defaults_to_primary_route -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex selected_model_defaults_to_minilm_modern -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex selected_model_accepts_legacy_l6_aliases -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex selected_model_accepts_l12_aliases -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex unknown_model_falls_back_to_default -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex bm25_weights_from_resolver_uses_defaults_when_env_missing -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex bm25_weights_from_resolver_applies_overrides_and_clamps -- --nocapture` (`1` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-19 20:05 - startup-loop bounded-retry hardening + graph refresh (local uncommitted batch):
  - `desktop/cortex-control-center/src/daemon-startup.js`, `src/daemon-startup.test.js`, `src/App.jsx`:
    - added `shouldContinueStartupRecovery(...)` and wired refresh logic so transient daemon-status managed-state drops no longer reset startup retry windows.
    - closes the long-tail case where the app can remain stuck on “Daemon is still starting. Reconnect will continue automatically.” indefinitely instead of timing out and surfacing actionable recovery.
    - added targeted regression coverage for managed-state drop continuity.
  - local runtime hygiene:
    - re-ran dev runtime cleanup sweep and verified stale wrapper session directories are cleared (`~/.cortex/runtime/control-center-dev/session-*` -> `0`).
  - graphify:
    - refreshed repository graph with `rtk graphify update C:\\Users\\aditya\\cortex` (`12,465` nodes, `48,672` edges, `558` communities); no new high-risk cross-module vulnerability edges were surfaced in this pass.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center run test -- src/daemon-startup.test.js src/analytics-projection.test.js src/number-format.test.js` (`21` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml event_pressure -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml run_event_compaction_cleanup_dry_run_reports_preview_lines -- --nocapture` (`1` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)

- 2026-04-19 21:15 - event-pressure remediation workflow + diagnostics (local uncommitted batch):
  - `daemon-rs/src/compaction.rs`:
    - exposed non-boot event-pressure thresholds as public constants and added explicit non-boot event pressure classification (`normal`/`elevated`/`critical`).
    - surfaced non-boot event count helper for cross-module diagnostics.
  - `daemon-rs/src/main.rs`:
    - `cortex doctor` now reports event-pressure state, non-boot event row counts, `decision_stored` row counts, and top event-type contributors so oversized event growth is visible without ad-hoc SQL.
    - `cortex cleanup` now supports one-time event remediation mode:
      - `--events` enables event-table compaction remediation.
      - `--dry-run` previews remediation impact.
      - `--max-passes <n>` bounds repeated governor passes for oversized histories.
    - cleanup help text/usage updated accordingly.
    - added regression tests for event-count helpers and event cleanup dry-run reporting.
  - `docs/internal/CORTEX-UNIFIED-STATUS-PLAN.md`:
    - execution-order event-volume loop updated to reflect landed diagnostics + remediation command, leaving only large-snapshot responsiveness verification as partial.
  - Validation:
    - `rtk cargo fmt --manifest-path daemon-rs/Cargo.toml`
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml event_ -- --nocapture` (`14` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)
    - `rtk npm --prefix desktop/cortex-control-center run test -- src/daemon-startup.test.js src/analytics-projection.test.js src/number-format.test.js` (`18` passing)
    - note: full `rtk cargo test --manifest-path daemon-rs/Cargo.toml` exceeded the 120s shell-call ceiling in this session; targeted changed-surface coverage and clippy are green.

- 2026-04-19 20:05 - embeddings migration closeout CLI (`87b0b01`):
  - `daemon-rs/src/main.rs`:
    - added `cortex embeddings status [--json]` to report active-model backlog counts for memories/decisions.
    - added `cortex embeddings drain [--batch-size <n>] [--max-batches <n>] [--lock-wait-ms <n>] [--until-exhausted] [--max-iterations <n>] [--json]`.
    - drain path now exposes deterministic backlog completion controls instead of relying only on passive startup/background drains.
    - added model-aware backlog counting helper (`count_unembedded_targets_for_model`) and regression coverage for model-specific pending counts.
  - Validation:
    - `rtk cargo fmt --manifest-path daemon-rs/Cargo.toml`
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml count_unembedded_targets_for_model_reports_model_specific_backlog -- --nocapture` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --bin cortex` (`382` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)
    - `rtk cargo run --manifest-path daemon-rs/Cargo.toml -- embeddings status --json` (pass)
    - `rtk cargo run --manifest-path daemon-rs/Cargo.toml -- embeddings drain --json --max-batches 1` (pass)

- 2026-04-19 19:15 - app-managed daemon startup/runtime sweep hardening (`4bde521`):
  - `desktop/cortex-control-center/src-tauri/src/main.rs`:
    - daemon lifecycle now clears stale managed-child handles on poll errors instead of leaving the app in a stale "starting" state.
    - desktop startup now prefers app-managed local daemon ensure first; service ensure fallback is explicit opt-in (`CORTEX_ALLOW_SERVICE_ENSURE_FALLBACK=1`) instead of default behavior.
    - stop/restart paths now fail closed with handle cleanup when child poll/kill operations fail.
  - `desktop/cortex-control-center/scripts/ensure-daemon-dev-binary.mjs`:
    - prebuild path now proactively sweeps conflicting dev daemons on Windows (stale `target/debug/cortex.exe` and stale `~/.cortex/runtime/control-center-dev/session-*` wrappers).
    - lock-retry flow now reruns the same sweep before a single rebuild retry.
  - Validation:
    - `rtk cargo fmt --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml`
    - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` (`22` passing)
    - `rtk cargo clippy --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml -- -D warnings` (pass)
    - `rtk npm --prefix desktop/cortex-control-center test -- src/daemon-startup.test.js src/api-client.test.js src/analytics-projection.test.js src/number-format.test.js` (`61` passing)
    - `rtk npm --prefix desktop/cortex-control-center run verify:lifecycle:dev` (pass)

- 2026-04-19 18:42 - matrix runner skip-missing dataset prerequisites (`ed0995f`):
  - `benchmarking/run_amb_cortex.py`:
    - matrix preflight now treats known dataset-prerequisite/import gaps as per-case skip conditions and continues matrix execution/summary generation instead of hard-failing the entire run.
    - skip reasons are surfaced in matrix outcomes so missing optional datasets are explicit and auditable.
  - `benchmarking/adapters/tests/test_run_amb_cortex_matrix.py`:
    - added/updated regressions for missing-prerequisite skip behavior and continue-on-error summary expectations.
  - Validation:
    - `rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_matrix.py benchmarking/adapters/tests/test_run_amb_cortex_shims.py -q` (`74` passing)
    - `rtk python -m pytest benchmarking/adapters/tests -q` (`155` passing)

- 2026-04-19 18:20 - Monte Carlo projection precision hardening (`7abc919`):
  - `desktop/cortex-control-center/src/analytics-projection.js`:
    - projected gain/run-rate values now route through bounded precision before display formatting, preventing long mantissa/scientific-notation artifacts from sparse-history drift.
  - `desktop/cortex-control-center/src/analytics-projection.test.js`:
    - added regression coverage for precision/format stability on high-magnitude projections.

- 2026-04-19 18:05 - stale managed-daemon recovery on desktop startup (`abfd03d`):
  - `desktop/cortex-control-center/src/App.jsx` + `src-tauri/src/main.rs`:
    - startup/reconnect flow now recovers from stale managed-daemon state transitions instead of remaining pinned in repeated "Daemon is still starting" retries.
    - daemon-status transitions are now reconciled with health/reachability checks before declaring startup failure.

- 2026-04-19 17:24 - BM25 runtime override controls for faster matrix iteration (`360c55b`):
  - `daemon-rs/src/handlers/recall.rs`:
    - added runtime-configurable BM25 field-weight resolver (`OnceLock` cached) for memories/decisions FTS ranking.
    - new env knobs:
      - `CORTEX_BM25_MEM_TEXT_WEIGHT`
      - `CORTEX_BM25_MEM_SOURCE_WEIGHT`
      - `CORTEX_BM25_MEM_TAGS_WEIGHT`
      - `CORTEX_BM25_DECISION_WEIGHT`
      - `CORTEX_BM25_CONTEXT_WEIGHT`
    - override parsing now fails safe (invalid/non-positive values revert to defaults) and clamps to bounded range (`0.1..12.0`) to prevent pathological runtime settings.
    - added regressions for default fallback and override/clamp behavior.
  - Validation:
    - `rtk cargo fmt`
    - `rtk cargo test handlers::recall::tests::` (`97` passing)
    - `rtk cargo test` in `daemon-rs` with isolated target dir (`404` passing)
    - `rtk cargo clippy -- -D warnings` in `daemon-rs` with isolated target dir (pass)

- 2026-04-19 17:10 - desktop dev-runtime cleanup hardening (`70e24a9`):
  - `desktop/cortex-control-center/scripts/cleanup-dev-runtime.mjs`:
    - expanded Windows predev cleanup to prune stale `~/.cortex/runtime/control-center-dev/session-*` wrapper directories.
    - cleanup now kills wrapper-owned stale processes before deleting session folders, and reports cleaned session counts in command output.
    - return payload now includes `cleanedSessions` and marks cleanup success when either stale processes or stale wrapper folders were removed.
  - Validation:
    - `rtk node desktop/cortex-control-center/scripts/cleanup-dev-runtime.mjs`
    - `rtk npm --prefix desktop/cortex-control-center test -- src/api-client.test.js src/daemon-startup.test.js src/analytics-projection.test.js src/number-format.test.js` (`60` passing)

- 2026-04-19 16:48 - sqlite-vec primary-route hydration + deterministic ordering (`7461e85`):
  - `daemon-rs/src/handlers/recall.rs`:
    - added `sqlite_vec_source_fallback_candidate` to hydrate shadow-only vec0-ranked sources from persisted memory/decision rows (`memory::id`, memory source, `decision::id`, decision context fallback).
    - sqlite-vec trial/primary route ordering now fills missing shadow-ranked sources with hydrated records (or bounded synthetic fallback), then appends remaining baseline candidates in original deterministic order.
    - added regression `maybe_apply_sqlite_vec_primary_includes_shadow_only_sources` to verify primary-mode routing admits shadow-only persisted sources while preserving expected top ordering.
  - Validation:
    - `rtk cargo test maybe_apply_sqlite_vec_primary_includes_shadow_only_sources` (`1` passing)
    - `rtk cargo test maybe_apply_sqlite_vec_trial_` (`2` passing)
    - `rtk cargo test handlers::recall::tests::` (`95` passing)
    - `rtk cargo test` in `daemon-rs` with isolated target dir (`402` passing)
    - `rtk cargo clippy -- -D warnings` in `daemon-rs` with isolated target dir (pass)

- 2026-04-19 16:00 - sqlite-vec route-mode rollout hardening + startup re-embed drain controls:
  - `daemon-rs/src/state.rs`, `daemon-rs/src/handlers/recall.rs`, `daemon-rs/src/handlers/health.rs`:
    - added explicit sqlite-vec route modes (`baseline`, `trial`, `primary`) with fail-closed `force_off` behavior and deterministic effective-mode resolution.
    - semantic recall + unified recall traces now emit route metadata (`routeMode`, effective trial percentage, route reason) to make rollout behavior auditable in telemetry and explain payloads.
  - `daemon-rs/src/handlers/recall.rs`:
    - shadow sqlite-vec KNN diagnostics now use prepared statements/params for vector literal + `k` binding instead of string-built SQL.
  - `daemon-rs/src/main.rs`:
    - embedding backfill pass no longer exits early when only one table still has full batches.
    - added optional startup extended drain controls:
      - `CORTEX_EMBED_BACKFILL_DRAIN_ON_STARTUP=1`
      - `CORTEX_EMBED_BACKFILL_STARTUP_DRAIN_MAX_BATCHES` (clamped, safe default)
    - added regression coverage for the new batch-continuation decision helper.
  - `daemon-rs/src/service.rs` + `desktop/cortex-control-center/src-tauri/src/main.rs`:
    - Windows hidden command/spawn paths now consistently use `CREATE_NO_WINDOW` without detached-console behavior, reducing ghost terminal flashes during service/app-managed startup flows.
  - `desktop/cortex-control-center/src/analytics-projection.js` + `src/number-format.js` (+ tests):
    - Monte Carlo and compact-number rendering hardening remains in place (bounded projections + no scientific notation leakage on large values).
  - Validation:
    - `rtk cargo fmt --manifest-path daemon-rs/Cargo.toml`
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-work backfill_batch_may_have_more_only_when_a_table_hits_limit` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-work collect_unembedded_targets_for_model` (`2` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-work sqlite_vec_canary_config_route_mode_aliases_are_supported` (`1` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-work maybe_apply_sqlite_vec_trial_` (`2` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-work service::tests::` (`9` passing)
    - `rtk npm --prefix desktop/cortex-control-center test -- src/analytics-projection.test.js src/number-format.test.js src/daemon-startup.test.js` (`15` passing)
    - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml --target-dir desktop/cortex-control-center/src-tauri/target-codex-work` (`21` passing)
    - full `daemon-rs` suite rerun attempted but exceeded lean-ctx shell timeout window (`120s`) in this session.

- 2026-04-19 13:30 - Control Center transport/UX stabilization follow-up:
  - `desktop/cortex-control-center/src-tauri/src/main.rs` (`794cebb`):
    - Windows app builds now force GUI subsystem in non-test mode (`windows_subsystem = "windows"`), suppressing ghost console/terminal windows during dev launch.
    - partial-response timeout classification now treats Windows `WSAETIMEDOUT` (`os error 10060`) as timeout-equivalent when bytes were already received.
    - tray initialization remains gated to hide-to-tray builds only, avoiding stale tray-icon churn in debug restart cycles.
  - `desktop/cortex-control-center/src/api-client.js` + `src/api-client.test.js` (`794cebb`):
    - IPC fallback now catches raw transport-layer failures (`Read failed`, `Write failed`, connect/setup timeout errors) and retries via local HTTP instead of surfacing hard dashboard errors.
    - added GET/POST regressions for raw read-failure fallback behavior.
  - `desktop/cortex-control-center/src/analytics-projection.js`, `src/number-format.js`, `src/App.jsx` + tests (`341cafd`):
    - Monte Carlo projection now clamps sparse-history drift/volatility to prevent explosive `e+16` projections from short noisy windows.
    - projection metrics now render with bounded compact suffix formatting (K/M/B/T/Q), eliminating scientific-notation leakage in mission-control/analytics cards.
    - added deterministic projection and number-format unit coverage.
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center test` (`75` passing)
    - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` (`21` passing)

- 2026-04-19 08:20 - Strict benchmark closure + startup lifecycle fallback hardening + live DB cleanup verification:
  - `benchmarking/run_amb_cortex.py`:
    - tightened LongMemEval short-answer shim behavior so singular item questions stay primary-item only (prevents over-complete item+accessory answers on raw path).
  - Benchmark outcomes (strict fair runs, no gate bypass):
    - tuned path (`cortex-http`): `benchmarking/runs/amb-run-20260419-073924` -> `20/20`, gate passed (`avg_recall_tokens=199.35`, `max_recall_tokens=294`, `score_per_1k_recall_tokens=5.0163`)
    - raw no-helper path (`cortex-http-base`): `benchmarking/runs/amb-run-20260419-074548` -> `20/20`, gate passed (`avg_recall_tokens=201.95`, `max_recall_tokens=297`, `score_per_1k_recall_tokens=4.9517`)
  - `desktop/cortex-control-center/src/App.jsx`:
    - lifecycle verification now falls back to polling if startup event stream init fails, avoiding false startup-failure reports in local dev lifecycle runs.
  - `desktop/cortex-control-center/src/daemon-startup.js` + `desktop/cortex-control-center/src/daemon-startup.test.js`:
    - centralized bounded startup retry windows/delay policy with dedicated unit coverage.
  - `desktop/cortex-control-center/src-tauri/src/main.rs`:
    - PATH binary fallback is now explicit opt-in via `CORTEX_ALLOW_PATH_BINARY_FALLBACK`.
  - `daemon-rs/src/compaction.rs`, `daemon-rs/src/db.rs`, `daemon-rs/src/handlers/health.rs`:
    - clippy-hardening cleanups (complex-type aliasing, dead-code pruning, explicit filter paths) while preserving behavior.
  - Live DB remediation (backup-first) was revalidated against benchmark namespace bloat:
    - backups: `~/.cortex/backups/cortex-pre-benchmark-cleanup-20260419-075715.db`, `~/.cortex/backups/cortex-pre-benchmark-cleanup-2-20260419-075738.db`
    - post-cleanup live counts: events `597`, decisions `489`, embeddings `9,749`, cluster_members `5,142`
    - local DB size: `720.93 MB` (backup snapshot) -> `386.37 MB` (active DB)
  - Validation:
    - `rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py -q` (`48` passing)
    - `rtk python -m pytest benchmarking/adapters/tests/test_cortex_http_base_provider.py benchmarking/adapters/tests/test_cortex_http_client.py benchmarking/adapters/tests/test_run_amb_cortex_matrix.py benchmarking/adapters/tests/test_run_amb_cortex_shims.py -q` (`149` passing)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`399` passing)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)
    - `rtk npm --prefix desktop/cortex-control-center test -- src/daemon-startup.test.js src/api-client.test.js` (`48` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)
    - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` (`21` passing)

- 2026-04-19 03:15 - Live DB compression tranche: payload compaction + savings rollups + orphan crystal-member pruning:
  - `daemon-rs/src/handlers/mod.rs`:
    - added write-path event payload compaction for high-churn telemetry:
      - `recall_query` now stores compact analytics-safe fields only
      - `merge` now stores bounded preview + `incoming_chars` instead of full raw text
      - large telemetry payloads now pass through a hard JSON-size budget with analytics-field preservation fallback
    - added regressions:
      - large merge payload compaction
      - recall analytics payload retention under compacted size budget
  - `daemon-rs/src/compaction.rs`:
    - added old savings-event rollup pass (`recall_query` / `store_savings` / `tool_call_savings`) into persistent `event_savings_rollups` (day/hour/operation aggregates), then deletes raw rolled rows
    - added orphan `cluster_members` pruning for missing memory/decision/cluster targets
    - extended compaction result/reporting with orphan-cluster-member pruning count
    - added regressions:
      - savings-event rollup compaction correctness
      - orphan cluster-member pruning correctness
  - `daemon-rs/src/db.rs`:
    - added `event_savings_rollups` table + supporting indexes for compact long-window savings analytics
  - `daemon-rs/src/handlers/health.rs`:
    - `/savings` now combines rollup aggregates with recent raw events for:
      - by-operation totals
      - daily cumulative savings
      - recall hit/miss trend
      - activity heatmap
    - preserves operator-facing 30-day analytics while reducing scan pressure on high-volume raw event history
  - Validation:
    - `rtk cargo fmt`
    - `rtk cargo test compaction:: -- --nocapture` (`14` passing)
    - `rtk cargo test log_event_ -- --nocapture` (`2` passing)
    - `rtk cargo test health:: -- --nocapture` (`10` passing)
    - `rtk cargo test --quiet` attempted; exceeded lean-ctx shell timeout (`105s`) before suite completion

- 2026-04-19 01:20 - Live DB anti-ballooning compaction + write-path event-cap hardening:
  - `daemon-rs/src/compaction.rs`:
    - expanded per-type caps to include high-volume telemetry families (`agent_boot`, `store_savings`, `tool_call_savings`) in both soft and hard pressure modes
    - corrected historical event pruning semantics:
      - stale `agent_boot` rows are now pruneable by retention/cap rules
      - `boot_savings_rollup` is now explicitly preserved from generic old-event deletion
    - global non-savings overflow and pressure-count paths now exclude only `boot_savings` and `boot_savings_rollup`, preventing non-savings telemetry from bypassing pressure controls
    - added/updated regressions for:
      - stale-event pruning behavior with old `agent_boot`
      - nonboot overflow semantics with preserved savings rows
      - explicit retention of `boot_savings_rollup` rows under old-event pruning
  - `daemon-rs/src/handlers/mod.rs`:
    - replaced single-type `decision_stored` write-time prune guard with a shared high-volume event-cap table
    - write-time pruning now applies to multiple noisy event families on the same interval boundary, reducing event-table balloon risk between background compaction passes
  - Validation:
    - `rtk cargo test compaction:: -- --nocapture` (`12` passing)
    - `rtk cargo test prune_event_type_keep_latest_trims_old_rows -- --nocapture` (`1` passing)
    - `rtk cargo test --quiet` attempted but exceeded lean-ctx shell timeout (`105s`) before completion; targeted suites above are green

- 2026-04-18 23:47 - Control Center startup unblock + IPC fallback resiliency:
  - `desktop/cortex-control-center/src/api-client.js`:
    - added defensive HTTP fallback path for Tauri IPC failures in both `createApi` and `createPostApi`
    - fallback now activates for IPC timeout/task-envelope failures (`IPC request ... timed out`, `... task failed`, invalid IPC envelope)
    - when IPC fallback also fails, error now preserves both causes (`IPC...; HTTP fallback failed: ...`) for operator debugging
    - IPC timeout routing now normalizes absolute URL-style paths before endpoint classification, preventing core routes from falling back to generic 8s timeout buckets
  - `desktop/cortex-control-center/src/App.jsx`:
    - startup refresh now applies health-based reachability fallback when `daemon_status` reports unreachable but `/health` is healthy
    - this prevents endless “Daemon is still starting...” loops caused by daemon-status false negatives while the daemon is actually online
    - fallback marks IPC fallback state in status messaging so degraded transport is visible without blocking app use
  - `desktop/cortex-control-center/src/api-client.test.js`:
    - added explicit regression coverage for GET/POST IPC-timeout fallback to HTTP
    - adjusted prior timeout/invalid-envelope tests to validate expected throw behavior when fallback transport is intentionally unavailable
  - Runtime remediation performed on local workstation:
    - identified 7 stale duplicate `cortex.exe mcp --agent codex` processes
    - terminated stale MCP duplicates, leaving only the app-managed serve daemon (`target-control-center-dev\\debug\\cortex.exe serve ... --port 7437 --bind 127.0.0.1`)
    - direct endpoint probe confirmed healthy core and work routes (`/sessions`, `/locks`, `/tasks`, `/feed`, `/messages`, `/activity`, `/conflicts`, `/permissions`) with `/savings` stable but slower as expected
  - Validation:
    - `rtk npm --prefix desktop/cortex-control-center run test -- src/api-client.test.js` (`40` passing)
    - `rtk npm --prefix desktop/cortex-control-center run web:build` (pass)

- 2026-04-18 23:25 - Raw benchmark backend path (no helper client logic) + truthful base baseline capture:
  - `benchmarking/adapters/cortex_http_base_provider.py` (new):
    - added direct HTTP AMB memory provider (`cortex-http-base`) that uses raw `/store` + `/recall` calls without `CortexHTTPClient` rerank/detail-variant helper logic
    - emits compatible retrieval metrics rows for gate accounting (`token_estimate`, `recall_call_count`, budgets, sampled sources)
  - `benchmarking/run_amb_cortex.py`:
    - added configurable benchmark backend selection (`--memory-backend`, env `CORTEX_BENCHMARK_MEMORY_BACKEND`)
    - registered both providers: `cortex-http` (tuned helper adapter) and `cortex-http-base` (raw direct path)
    - matrix schema/args now support `memory_backend` defaults and per-case overrides
    - baseline scenario keys now include backend suffix (`...::<memory_backend>`) with legacy fallback lookup for existing entries
    - run manifest + matrix dry-run/defaults now record selected memory backend
  - tests:
    - new `benchmarking/adapters/tests/test_cortex_http_base_provider.py` for raw provider ingest/recall/metrics behavior
    - expanded parser/backend coverage in `benchmarking/adapters/tests/test_run_amb_cortex_shims.py`
    - full adapter suite validation: `rtk python -m pytest benchmarking/adapters/tests -q` (`142` passing)
  - raw/no-python-helper benchmark evidence:
    - daemon-level raw benchmark: `rtk cargo test --manifest-path daemon-rs/Cargo.toml --test recall_benchmark -- --nocapture` (`7` passing)
  - strict base backend LongMemEval evidence (no helper client path):
    - run: `rtk python benchmarking/run_amb_cortex.py run --dataset longmemeval --split s --query-limit 20 --max-runtime-seconds 1200 --memory-backend cortex-http-base`
    - artifact: `benchmarking/runs/amb-run-20260418-230402`
    - outcome: `4/20`, `accuracy=0.2`, gate failed
    - gate details: `avg_recall_tokens=267.3`, `max_recall_tokens=300`, failures = accuracy floor miss + avg recall token ceiling miss
    - this run is intentionally retained as an honest no-helper baseline (no test or benchmark fudging)

- 2026-04-18 22:20 - Runtime binary-source hardening + build-bloat guardrails:
  - `desktop/cortex-control-center/src-tauri/src/main.rs`:
    - strengthened daemon binary path denylist so non-runtime isolated target trees are rejected by default
    - `target-*` directories are now disallowed unless explicitly runtime-safe (`target-control-center-dev`, `target-control-center-release`)
    - startup path guard now blocks `target*/build` and `target*/incremental` artifacts in addition to existing test/deps protections
    - added regression coverage for blocked paths (`target-rtk-isolated`, `target-codex-test`, `target/debug/build`) while preserving allowed runtime paths
  - `.gitignore`:
    - added explicit ignore coverage for recurring transient workspace noise (`target-tests`, `src-tauri/target-tests`, `pytest-cache-files-*`, `Usersadityacortex.tmppytest`)
  - `.ignore` (new):
    - added repo-wide search-ignore guardrails for heavy generated artifacts (Rust targets, benchmark tools/runs, graph outputs, cache folders) to reduce repeated large-tree scans in tooling
  - `scripts/prune-build-bloat.ps1` (new):
    - added measured dry-run/apply cleanup utility for stale target/test/benchmark artifacts with age-aware runtime target handling
    - added root npm wrappers in `package.json`:
      - `ops:prune-build-bloat`
      - `ops:prune-build-bloat:apply`
  - `desktop/cortex-control-center/package.json`:
    - aligned daemon build helper scripts to isolated control-center target dirs (`target-control-center-dev` / `target-control-center-release`) instead of the shared default target dir
  - Local measurement snapshot from dry-run:
    - reclaimable: `22.044 GB`
    - largest candidates: `daemon-rs/target-tests` (`5.068 GB`), `target-rtk-isolated` (`4.923 GB`), `target-codex-test` (`4.916 GB`), `target-rtk-audit` (`3.584 GB`), `src-tauri/target-tests` (`2.142 GB`)
- Validation:
  - `rtk cargo fmt --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml`
  - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml disallowed_daemon_binary_path_blocks_wrappers_temp_and_test_artifacts -- --nocapture` (`1` passed)
  - `npm run -s ops:prune-build-bloat` (dry-run successful with candidate report)

- 2026-04-18 22:50 - Benchmark qualifier precision + git perf audit automation:
  - `benchmarking/adapters/cortex_http_client.py`:
    - added study-abroad location qualifier augmentation pass (`_augment_abroad_location_qualifier`) after location rerank/promotion so top context can include a missing country qualifier when one strong sibling-memory candidate exists
    - added country-like location term filter (`_is_country_like_location_term`) to avoid generic place-hint terms
  - `benchmarking/adapters/tests/test_cortex_http_client.py`:
    - added regression `test_recall_documents_location_queries_append_abroad_country_qualifier_to_primary_context`
  - strict benchmark validation:
    - run: `rtk python benchmarking/run_amb_cortex.py run --dataset longmemeval --split s --query-limit 20 --max-runtime-seconds 1200`
    - artifact: `benchmarking/runs/amb-run-20260418-224211`
    - outcome: `20/20`, `accuracy=1.0`, gate passed, `avg_recall_tokens=191.1`, `max_recall_tokens=295`, `over_budget_count=0`
  - git/env slowdown remediation tooling:
    - added `scripts/git-perf-health.ps1` (status timing, object-store health, bloat directory sizing, optional local git perf knob apply)
    - added npm scripts in `package.json`:
      - `ops:git-perf-audit`
      - `ops:git-perf-apply`
    - extended root `.gitignore` with temp-noise excludes from real status warnings:
      - `.tmp/pytest/`
      - `tmp/ptbase-*/`
      - `tmp/pytest-local/`
    - local audit/after-apply snapshot: `git status -sb` ~`25ms`, object store ~`152.51 MiB`; primary bloat remains stale target/test artifact directories

- 2026-04-18 18:20 - Startup timeout-storm hardening + daemon startup scheduling safety pass:
  - `desktop/cortex-control-center/src/App.jsx`:
    - startup protected refresh now prioritizes core routes (`/sessions`, `/locks`, `/tasks`) and defers secondary routes (`/feed`, `/messages`, `/activity`, `/conflicts`, `/permissions`) to background on first successful core hydration
    - secondary timeout failures now surface partial-availability feedback instead of forcing global-offline state
  - `desktop/cortex-control-center/src-tauri/src/main.rs`:
    - daemon binary path guard now rejects non-runtime test artifact paths (`target-tests`, `target-test`, `nextest`, `target*/deps`) to prevent accidental app-managed launches from test binaries
  - `daemon-rs/src/state.rs`:
    - replaced single read-connection mutex with pooled query-only read provider (`Arc<dyn ReadConnectionProvider>`)
    - runtime now opens a bounded read pool (`CORTEX_DB_READ_POOL_SIZE`, CPU-aware defaults/clamps) to reduce read lock contention under startup fanout
  - `daemon-rs/src/compaction.rs`:
    - added startup-safe governor mode (`run_compaction_governor_startup`) that relieves event pressure without VACUUM
  - `daemon-rs/src/main.rs`:
    - added startup schedule model and per-lane startup delay controls (`CORTEX_STARTUP_*_DELAY_SECS`)
    - added bounded opportunistic lock acquisition for background DB tasks (`CORTEX_BACKGROUND_DB_LOCK_MAX_WAIT_MS`) so non-critical jobs skip instead of queuing behind hot request locks
    - added startup storage-governor catch-up loop using startup-safe compaction for early event-pressure relief
    - added hard clamp for app-managed heavy startup delay (`APP_MANAGED_STARTUP_HEAVY_DELAY_MAX_SECS=120`) so misconfigured values (for example `777`) cannot defer stabilization for many minutes
    - stabilized startup preflight tests against live local IPC interference by explicitly disabling IPC endpoint for those fixtures
- Validation:
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml` (`374` passing)
  - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --test recall_benchmark -- --nocapture` (`7` passing)
  - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` (`16` passing)
  - `rtk npm test` in `desktop/cortex-control-center` (`57` passing)
  - `rtk npm run web:build` in `desktop/cortex-control-center` (pass)
  - `rtk python benchmarking/run_amb_cortex.py matrix --dry-run --matrix-file benchmarking/configs/amb-eval-matrix.nonlongmem.q5.json` (strict fair-run preflight passed)
  - `rtk python benchmarking/run_amb_cortex.py matrix --matrix-file benchmarking/configs/amb-eval-matrix.nonlongmem.q5.json --continue-on-error --summary-file benchmarking/runs/matrix-summary-latest-q5.json` (expected fail-fast: no answer/judge provider keys configured)
- Notes:
  - `benchmarking/adapters/tests/test_cortex_http_client.py` currently reports multiple failing cases in this workspace (`_FakeHTTPXClient` queue exhaustion and retrieval-policy expectation mismatch). These failures were observed during validation but are outside the files changed in this optimization batch.

- 2026-04-18 18:45 - Savings telemetry realism + benchmark bloat remediation pass:
  - Operational live-DB remediation (`~/.cortex/cortex.db`) executed with backup:
    - backup created: `~/.cortex/backups/cortex-pre-amb-cleanup-20260418-181602.db`
    - deleted benchmark namespace rows (`source_agent = 'amb-cortex' OR 'amb-cortex::%'`) across decisions + linked decision embeddings + JSON-encoded event rows
    - before/after totals:
      - events: `433,479 -> 1,133`
      - decisions: `31,185 -> 489`
      - decision embeddings: `11,946 -> 483`
    - purged legacy `boot_savings` rows generated by pre-fix baseline accounting (`441` rows), reducing total events to `692` and resetting inflated dashboard savings history
  - `daemon-rs/src/compiler.rs`:
    - boot savings baseline estimation no longer counts filesystem-size heuristics from custom source directories
    - baseline now reflects DB-backed boot context families actually assembled into prompt flow (active memories + active decisions), preventing exaggerated token/$ savings telemetry
  - `daemon-rs/src/indexer.rs`:
    - marked `custom_source_paths` helper with `#[allow(dead_code)]` after compiler baseline decoupling to keep strict warning gates clean
  - Validation:
    - protected endpoint latency spot-check after cleanup:
      - `/sessions`, `/locks`, `/tasks`, `/feed`, `/messages`, `/activity`, `/conflicts`, `/permissions`, `/savings` all returned `200` (roughly `0.9ms` to `34.8ms` in local probe)
    - `rtk cargo check --manifest-path daemon-rs/Cargo.toml` (pass)
    - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` (pass)
    - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --test recall_benchmark -- --nocapture` (`7` passing)
  - Commit:
    - pending (this optimization batch)

- Startup contention and high-event-volume analytics hardening:
  - `daemon-rs/src/handlers/health.rs` (`GET /savings`) now avoids full event-log Rust parsing under a long shared DB-read lock.
  - `/savings` operation rollups and trends are now SQL-aggregated in SQLite, with a short TTL payload cache to stabilize repeated dashboard refreshes.
  - `desktop/cortex-control-center/src/App.jsx` now excludes `/savings` from startup-critical protected fanout and refreshes it lazily when the Analytics panel is active.
  - This removes a major lock-contention path where heavy savings reads cascaded into 8s timeout storms on `/sessions`, `/locks`, `/tasks`, `/feed`, `/messages`, and `/activity`.
- Runtime diagnostics (live app DB) captured for root-cause clarity:
  - `~/.cortex/cortex.db` event volume observed at ~433k rows, dominated by `decision_stored` (~420k), with event window spanning 2026-03-28 through 2026-04-18.
  - On the same DB snapshot, a representative old-path full event fetch measured ~1.53s vs new aggregate-query bundle ~0.297s (local instrumentation pass).
- Validation:
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::health::tests:: -- --nocapture` (`9` passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo check --manifest-path daemon-rs/Cargo.toml` (pass)
  - `rtk npm test -- api-client.test.js` in `desktop/cortex-control-center` (`38` passing)
  - `rtk npm run web:build` in `desktop/cortex-control-center` (pass)

- Advanced Phase-2A power-user performance and fair-benchmark stability:
  - `daemon-rs/src/handlers/recall.rs` now applies `source_prefix` filtering directly in semantic/shadow SQL queries (memories + decisions) before vector scoring.
  - Semantic/shadow candidate collection now streams row iterators instead of building full intermediate vectors up-front.
  - Added regressions for scoped semantic and scoped shadow-semantic behavior (`semantic_candidate_collection_honors_source_prefix_scope`, `shadow_semantic_rows_honor_source_prefix_scope`).
  - `daemon-rs/src/embeddings.rs` now supports `CORTEX_EMBED_SESSION_POOL_SIZE` (default `2`, clamp `1..8`) for startup/throughput tuning.
  - `benchmarking/adapters/cortex_amb_provider.py` now defaults AMB provider concurrency to `1` with env override (`CORTEX_BENCHMARK_PROVIDER_CONCURRENCY`) to reduce local-daemon burst 429 artifacts.
  - Added Python regression coverage for concurrency default/override in `benchmarking/adapters/tests/test_run_amb_cortex_shims.py`.
- Validation:
  - `rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py sdks/python/tests/test_client.py -q` (`17` passing)
  - `rtk npm --prefix C:\\Users\\aditya\\cortex\\sdks\\typescript test` (`6` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml embeddings::tests -- --nocapture` (`7` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml handlers::recall::tests -- --nocapture` (`94` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture` (`7` passing)
- Commit:
  - `373dce0`

- Hardened Phase-2A benchmark hygiene for app-managed daemon mode:
  - `benchmarking/run_amb_cortex.py` now defaults benchmark runs to app-daemon-required attach mode and fails fast if no live Control Center daemon is reachable.
  - Added app-attached benchmark namespace cleanup on exit (run-scoped `source_agent=amb-cortex::<namespace>` decisions + linked decision embeddings + matching events), with per-run `namespace-cleanup.json` audit output.
  - Added regression test coverage for precise cleanup semantics in `benchmarking/adapters/tests/test_run_amb_cortex_shims.py`.
- Validation:
  - `rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py benchmarking/adapters/tests/test_cortex_http_client.py sdks/python/tests/test_client.py -q` (`21` passing)
  - `rtk npm --prefix C:\\Users\\aditya\\cortex\\sdks\\typescript test` (`6` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml handlers::store::tests -- --nocapture` (`19` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml embeddings::tests -- --nocapture` (`5` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml rate_limit::tests -- --nocapture` (`5` passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture` (`7` passing)

- Added first-class multi-dataset benchmark matrix support to AMB runner:
  - `benchmarking/run_amb_cortex.py` now supports `matrix` command with JSON case loading, per-case run arg synthesis, case-level isolated execution, dry-run previews, continue-on-error mode, and consolidated matrix summary output.
  - Added tracked stage-1 matrix spec in `benchmarking/configs/amb-eval-matrix.stage1.json` (LongMemEval, LoCoMo, MemBench splits, PersonaMem splits, MemSim splits, LifeBench, BEAM).
  - Added matrix helper unit tests in `benchmarking/adapters/tests/test_run_amb_cortex_matrix.py`.
- Validation:
  - `python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py benchmarking/adapters/tests/test_run_amb_cortex_matrix.py sdks/python/tests/test_client.py -q` (`12` passing)
  - `npm --prefix C:\\Users\\aditya\\cortex\\sdks\\typescript test` (`6` passing)
  - `python benchmarking/run_amb_cortex.py matrix --dry-run --matrix-file benchmarking/configs/amb-eval-matrix.stage1.json` (validated full case expansion)
  - `python benchmarking/run_amb_cortex.py matrix --matrix-file benchmarking/configs/amb-eval-matrix.stage1.json --query-limit 5 --continue-on-error --no-enforce-gate --allow-missing-recall-metrics --summary-file benchmarking/runs/matrix-summary-latest-q5.json` (all cases clean-failed due missing provider key; no scored output fabricated)
- Commit:
  - `f6016f4`

- Advanced Phase-2A benchmark/runtime quality + token-efficiency groundwork across AMB + SDK surfaces:
  - `benchmarking/run_amb_cortex.py` now applies runtime compatibility shims for pinned dataset variants that do not accept `user_ids`, with best-effort user filtering on returned docs.
  - LongMemEval prompt flow is forced to keep retrieved context primary; compact retrieval metrics are appended as telemetry context instead of replacing memory text with raw payload JSON.
  - Added prompt-ready recall-context helpers in both SDKs:
    - Python: `format_recall_context`, `recall_for_prompt`
    - TypeScript: `formatRecallContext`, `recallForPrompt`
  - Added regression coverage:
    - `benchmarking/adapters/tests/test_run_amb_cortex_shims.py`
    - `sdks/python/tests/test_client.py`
    - `sdks/typescript/test/client.test.mjs`
- Validation:
  - `python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py sdks/python/tests/test_client.py -q` (`8` passing)
  - `npm --prefix C:\\Users\\aditya\\cortex\\sdks\\typescript test` (`6` passing)
  - `python benchmarking/run_amb_cortex.py smoke` (isolated daemon smoke retrieval pass)
- Commit:
  - `3d75bdd`

- Expanded provider-free benchmark guardrails in `daemon-rs/tests/recall_benchmark.rs`:
  - added regression metrics for top-1 hit rate, recall coverage, p95 query tokens, and tokens-per-relevant-hit
  - added explain accounting invariants (`spent` equals returned token sum; pre-budget drop math parity)
  - added ranking-factor presence checks to catch explain payload drift (`tokenCost`, `budgetCostRatio`, `entityMatches`)
- Validation:
  - `rtk cargo fmt --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml` (`351` tests passing)
  - `rtk cargo clippy --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml -- -D warnings`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml --test recall_benchmark -- --nocapture` (`7` tests passing)
- Commit:
  - `0b41668`
- Advanced Phase-2A retrieval quality/token-efficiency calibration in `daemon-rs/src/handlers/recall.rs`:
  - semantic budget packing now adapts relevance floor, max-items, and excerpt caps by query shape
  - memory/decision fallback ranking (FTS + non-FTS) now uses adaptive keyword/score/recency/retrieval weighting
  - empty-term fallback ordering now prioritizes retained score signal before pure recency
  - added regressions for adaptive fallback weighting, query-adaptive cap behavior, and score-aware empty-term ordering
- Validation:
  - `rtk cargo fmt --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml` (`351` tests passing)
  - `rtk cargo clippy --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml -- -D warnings`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml --test recall_benchmark -- --nocapture` (`7` tests passing)
- Commit:
  - `f6f878d`
- Stabilized cross-process global-lock regression behavior under full suite parallelism:
  - switched test coverage to explicit lock-path acquisition helper to eliminate shared-env (`CORTEX_GLOBAL_LOCK_HOME`) race coupling
  - preserves one-daemon lock semantics while making full-suite runs deterministic
- Validation:
  - `rtk cargo fmt --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml`
  - `rtk cargo clippy --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml -- -D warnings`
  - `rtk cargo test --manifest-path C:\Users\aditya\cortex\daemon-rs\Cargo.toml --test recall_benchmark -- --nocapture`
- Commit:
  - `95e215c`- Added admin remediation endpoint regressions with real DB-effect assertions:
  - `/admin/unowned` coverage for ownerless row counting
  - `/admin/assign-owner` coverage for owner reassignment behavior
  - `/admin/set-visibility` coverage for allowlisted visibility updates
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml handlers::admin::tests::handle_ -- --nocapture`
- Commit:
  - `9254c7c`
- Hardened singleton daemon reliability regressions (cross-process + contention + parent-death guardrails):
  - added cross-process global lock contention test coverage in `daemon-rs/src/auth.rs`
  - added concurrent startup-burst runtime lock contention coverage in `daemon-rs/src/main.rs`
  - added spawned-owner dead-parent claim rejection coverage in `daemon-rs/src/main.rs`
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml acquire_global_daemon_lock_rejects_cross_process_duplicate_instances -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml acquire_runtime_lock_rejects_concurrent_startup_burst -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml spawned_owner_runtime_claim_rejects_dead_parent_process -- --nocapture`
- Commit:
  - `214f253`
- Hardened app-client singleton spawn policy fail-closed behavior:
  - when `CORTEX_APP_CLIENT` is present but explicit local-spawn policy is missing, daemon startup now denies auto-spawn by default
  - prevents partial env contracts from accidentally re-enabling local daemon creation in app-managed flows
- Added policy-focused regressions:
  - app-marked + missing spawn contract => denied
  - app-marked + explicit local-spawn opt-in => allowed
  - app-required override => denied even with local-spawn opt-in
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex local_spawn_policy -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex ensure_daemon_app_required_policy_returns_machine_readable_error -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex` (`274` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `c8d5fb0`
- Added rank-delta drift metrics to shadow semantic parity diagnostics:
  - `build_shadow_semantic_explain` now emits `matchedRankPairs`, `meanAbsRankDelta`, and `top1Match` for successful probes
  - recall telemetry summary now preserves those metrics in compact `shadow_semantic` payloads
- Extended `/stats` shadow gate rollups and readiness criteria:
  - aggregate metrics now include `ok_mean_abs_rank_delta_avg` and `ok_top1_match_rate`
  - gate thresholds now include `max_mean_abs_rank_delta` and `min_top1_match_rate`
  - `shadow_semantic_gate` and `shadowSemanticGate` expose these values in both snake_case and camelCase surfaces
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex shadow_semantic_telemetry_summary_compacts_ok_payload -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex build_recall_stats_payload -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex` (`271` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `b06a056`
- Removed duplicate embedding work in recall explain paths:
  - `execute_recall_policy_explain_inner` now computes query embedding once and reuses it for both recall ranking and `shadowSemantic` diagnostics
  - headlines explain now uses traced recall path so semantic baseline metadata is reused instead of recomputed
  - budget explain now routes through query-vector trace path directly, eliminating extra engine embed calls
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex execute_recall_policy_explain -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex shadow_semantic_explain_uses_provided_baseline_override -- --nocapture`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex` (`271` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `6d19f9f`
- Enforced app-owned singleton attach flow across daemon/app/plugin surfaces:
  - daemon now treats app clients as attach-only when registration env contract is set and returns machine-readable `APP_INIT_REQUIRED` if app-managed daemon init is required
  - Control Center registration now persists and validates attach-only env contract in both JSON and TOML MCP server definitions (`CORTEX_APP_REQUIRED`, `CORTEX_DAEMON_OWNER_LOCAL_SPAWN`, `CORTEX_APP_CLIENT`)
  - plugin local scripts now emit explicit app-initialization guidance for attach-only client flows
- Hardened singleton lock tests under live app-daemon conditions:
  - runtime-lock tests now isolate `CORTEX_GLOBAL_LOCK_HOME`, removing false contention with active user daemon
  - test env lock guard now recovers from poison to prevent cascade failures after one panic
- Advanced sqlite-vec shadow KNN narrow-path integration without changing production routing:
  - recall trace now carries semantic baseline metadata into shadow explain (no duplicate baseline recomputation)
  - budget-trace empty-result branch now preserves semantic baseline metadata for consistent shadow diagnostics
  - added baseline-override regression coverage for shadow explain payloads
- Hardened benchmark harness for one-daemon policy:
  - `daemon-rs/tests/recall_benchmark.rs` now self-skips with explicit reason when a singleton daemon is already active, instead of failing due second-daemon startup denial
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --bin cortex` (`271` tests passing)
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center\\src-tauri\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center\\src-tauri\\Cargo.toml` (`15` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center\\src-tauri\\Cargo.toml -- -D warnings`
- Commit:
  - `b7e4b7b`
- Added embedding migration observability to `/health`:
  - `vector_search.embedding_inventory` now reports active/other/unknown model counts keyed to the selected profile
  - health now reports active-model coverage ratio plus re-embed backlog totals for memories and decisions
  - metrics are derived from selected-model coverage checks (`LOWER(model)` parity), so profile-switch progress is visible without changing retrieval routing
- Added regression coverage for mixed-model inventory and backlog accounting.
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`285` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `34b2bc6`
- Hardened embedding-profile migration safety and semantic quality gates:
  - embedding runtime/config now resolves a selectable profile via `CORTEX_EMBEDDING_MODEL` while keeping MiniLM as default
  - semantic and shadow-semantic collection now filter by selected model tag + expected vector dimensionality
  - startup backfill now re-queues rows missing the active model tag instead of only targeting rows with no embedding record
  - startup/store/MCP/crystallize write paths now persist model tag from engine identity (`engine.model_key()`)
- Added migration-focused regressions:
  - startup backfill selector coverage for mismatched-vs-matching model tags
  - explicit model-tag persistence coverage for decision embedding writes
  - embedding env tests now restore previous env state for reliable suite isolation
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`284` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-rtk-isolated'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `bc87c26`
- Added explicit vec0 shadow-routing gate diagnostics to `/stats`:
  - `shadow_semantic_gate` now emits threshold-based `hold` vs `ready_for_vec0_trial` decisions
  - gate output includes blocker reasons, observed sample counts, ok/unavailable/error rates, and overlap/jaccard quality metrics
  - camelCase compatibility alias (`shadowSemanticGate`) ships in parallel with snake_case output
- Added regression coverage for both gate paths:
  - sparse/mixed telemetry holds the gate
  - healthy high-overlap telemetry marks the gate ready for a controlled vec0 trial
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml build_recall_stats_payload -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml` (`279` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo clippy --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml -- -D warnings`
- Commit:
  - `d63257d`
- Added shadow-semantic parity rollups to `/stats` from unified `recall_query` telemetry:
  - status counts now aggregate `ok` / `unavailable` / `error` / `skipped`
  - overlap ratio and jaccard averages for `ok` probes are now computed for vec0 routing gate decisions
  - payload now emits both `shadow_semantic` and `shadowSemantic` aliases for compatibility
- Added/updated regression coverage:
  - recall stats unit test now asserts shadow status counts plus overlap/jaccard average rollups
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml build_recall_stats_payload_summarizes_tiers_and_latency -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml` (`278` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo clippy --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml -- -D warnings`
- Commit:
  - `52739fe`
- Expanded sqlite-vec shadow diagnostics into unified recall telemetry without switching production ranking:
  - `execute_unified_recall` now computes shadow semantic diagnostics for uncached/headlines requests and emits compact `shadow_semantic` fields in `recall_query` events
  - cache-hit events now explicitly report `shadow_semantic.status=skipped` with `reason=cache_hit`
  - event payloads intentionally omit baseline/shadow source arrays to keep telemetry token/storage overhead low
- Added regression coverage for all unified recall event branches:
  - compact shadow telemetry summary serialization
  - uncached telemetry payload assertions
  - cache-hit telemetry payload assertions
  - headlines-mode telemetry payload assertions
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml shadow_semantic -- --nocapture`
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml` (`278` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo test --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml --test recall_benchmark -- --nocapture` (`3` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\target-rtk-isolated'; rtk cargo clippy --manifest-path C:\\\\\\\\Users\\\\\\\\aditya\\\\\\\\cortex\\\\\\\\daemon-rs\\\\\\\\Cargo.toml -- -D warnings`
- Commit:
  - `3654260`
- Hardened shadow semantic status classification for sqlite-vec capability gaps:
  - known vec0 module-missing failures now resolve to `unavailable` rather than generic `error`
  - explain payload now returns stable reason `sqlite_vec_not_available` plus detail text for diagnostics
  - added direct regression coverage for missing-module error classification
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml --test recall_benchmark -- --nocapture`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml` (`274` tests passing)
  - `rtk cargo clippy --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml -- -D warnings`
- Commit:
  - `deeecb3`
- Fixed semantic candidate collection for solo-schema deployments:
  - `collect_semantic_candidates` now falls back to no-ACL projections when owner/visibility columns are absent
  - both memory and decision semantic candidate queries now preserve semantic retrieval in default solo-mode schema layouts
  - added regression coverage verifying expected embedding-backed semantic candidates still surface under solo schema
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml --test recall_benchmark -- --nocapture`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml` (`273` tests passing)
  - `rtk cargo clippy --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml -- -D warnings`
- Commit:
  - `f938ea0`
- Added sqlite-vec shadow KNN diagnostics on the explain path (no production ranking switch):
  - `GET /recall/explain` now emits `explain.shadowSemantic` with status (`ok` / `unavailable` / `error`)
  - diagnostic payload now includes baseline/shadow top-source slices plus overlap metrics (`overlapCount`, `overlapRatio`, `jaccard`)
  - shadow candidate collection now handles both team and solo schemas by falling back when owner/visibility columns are absent
  - vec0 shadow query path now uses explicit vector literals and bounded `k` to mirror proven sqlite-vec smoke-query behavior
- Added/updated regression coverage:
  - recall explain tests cover shadow diagnostic status behavior and registered-connection vec0 probe path
  - `daemon-rs/tests/recall_benchmark.rs` now enforces presence/shape of `shadowSemantic` diagnostics for each explain benchmark case
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml --test recall_benchmark -- --nocapture`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml` (`272` tests passing)
  - `rtk cargo clippy --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml -- -D warnings`
- Commit:
  - `06088fd`
- Added explicit distilled proxy-vs-full recall-ordering benchmark coverage:
  - `daemon-rs/tests/recall_benchmark.rs` now calls `/recall/explain` per benchmark case
  - added distilled 6-feature proxy scoring over explain factors (`relevance`, `budgetCostRatio`, `entropy`, `tokenCost`, `method=hybrid`, `method=semantic`)
  - added regression gates for proxy tracking quality (top-1 agreement floor + mean absolute rank error ceiling), with pairwise ordering agreement reported in assertion context
  - production recall behavior remains unchanged (test-only benchmark instrumentation)
- Validation:
  - `rtk cargo fmt --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml --test recall_benchmark -- --nocapture`
  - `rtk cargo test --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml` (`270` tests passing)
  - `rtk cargo clippy --manifest-path C:\\\\Users\\\\aditya\\\\cortex\\\\daemon-rs\\\\Cargo.toml -- -D warnings`
- Commit:
  - `39c32a2`
- Landed the first sqlite-vec foundation slice without switching production recall away from blob-scan:
  - daemon DB open now bootstraps the sqlite-vec auto-extension before runtime connections are created
  - health now reports sqlite-vec availability/version while the live vector backend remains `blob_scan`
  - added a real `vec0` smoke query against synthetic vectors so the repo proves sqlite-vec is actually wired on this stack
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`269` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `f7f3c3d`
- Aligned synonym-expanded term-group behavior across keyword retrieval instead of limiting it to the FTS `MATCH` planner:
  - fallback memory/decision ranking now scores matched term groups rather than literal raw tokens
  - post-FTS keyword ranking now counts the same synonym-expanded groups used to fetch the rows
  - query-focused excerpt centering now searches the same expanded term set, so shorthand queries can still lock onto expanded spans such as `db -> database`
- Added direct regressions for:
  - synonym-expanded excerpt centering
  - memory fallback term-group matching
  - decision fallback term-group matching
  - memory FTS scoring term-group matching
  - decision FTS scoring term-group matching
- Validation:
  - `rtk cargo fmt --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`268` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml --test recall_benchmark -- --nocapture`
- Commit:
  - `d26657a`
- Landed the first query-adaptive weighted-fusion pass in unified recall:
  - reciprocal-rank fusion now adjusts keyword vs semantic weight based on the query shape instead of treating both rankers as equally trustworthy for every prompt
  - short/exact/code-like queries now bias toward keyword retrieval
  - longer natural-language prompts bias toward semantic retrieval when embeddings are available
  - semantic-unavailable runs now degrade explicitly to keyword-only fusion
- Added regression coverage for:
  - heavier-ranker preference in weighted RRF
  - short/exact query weighting
  - long natural-language query weighting
  - semantic-unavailable weighting fallback
- Validation:
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`263` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `200fbef`
- Closed the remaining app-managed restart/reconnect verification gap:
  - added `desktop/cortex-control-center/scripts/run-dev-lifecycle-verification.mjs`
  - added `npm --prefix desktop/cortex-control-center run verify:lifecycle:dev` plus root alias `npm run desktop:verify:lifecycle:dev`
  - the runner boots the real Tauri app, restarts the daemon through the app surface, waits for disconnect/reconnect, runs `cortex_reconnect`, and verifies the final read path still preserves session truth
- Hardened daemon-side session truth during that verification:
  - model-less MCP read-path refresh now reuses the existing modeled session when possible instead of forking a second logical session row
  - reconnect/upsert paths now preserve richer existing session descriptions instead of overwriting them with weaker reconnect labels
  - added regressions for reconnect-preserved descriptions and model-less read refresh reuse
- Removed a Windows UX hazard from local app-managed startup verification:
  - hidden-process flags now cover `cortex paths --json` and `cortex service ensure` preflight subprocesses in the Control Center, not just the final daemon spawn
- Fixed two desktop issues surfaced by smoke during the same batch:
  - missing `/permissions` on older/mock daemons now degrades as an unsupported capability instead of spamming the dashboard with repeated warnings
  - Shared Feed kind-filter changes now trigger an immediate refresh, so the list actually follows the selected backend query
- Validation:
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`259` tests passing)
  - `$env:CARGO_TARGET_DIR='C:\\Users\\aditya\\cortex\\daemon-rs\\target-codex-validation'; rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center\\src-tauri\\Cargo.toml` (`13` tests passing)
  - `rtk npm --prefix C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center run test` (`53` tests passing)
  - `rtk npm --prefix C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center run verify:lifecycle:dev`
  - `rtk npm --prefix C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center run expect:smoke:ci`
- Commit:
  - `aebefce`
- Clarified the desktop app�s shipped lifecycle model so it matches the real one-daemon architecture:
  - About/lifecycle copy now calls the daemon app-managed instead of a sidecar
  - restart/exit wording now makes ownership and �no second instance� semantics explicit
  - added `desktop/cortex-control-center/DEVELOPING.md` with app-managed vs plugin-only guidance plus canonical dev/build/smoke commands
- Validation:
  - `npm --prefix C:\\Users\\aditya\\cortex run desktop:build` (web build, daemon release build, and desktop bundle build all passed; signing stopped as expected without `TAURI_SIGNING_PRIVATE_KEY`)
  - `npm --prefix C:\\Users\\aditya\\cortex\\desktop\\cortex-control-center run expect:smoke:ci` (About wording verified, mobile verified, vitest healthcheck passed with `53` tests)
- Commit:
  - `3c949e5`
- Closed the daemon-side session-truth gap for MCP memory tools:
  - `cortex_boot` now emits a real `session` event so the control center refreshes agent presence immediately
  - read-path tools (`cortex_peek`, `cortex_recall`, `cortex_recall_policy_explain`, `cortex_semantic_recall`, `cortex_unfold`) recreate missing MCP session rows and refresh heartbeat/expiry on demand
  - existing boot-time descriptions are preserved during read-path refresh, so session truth stays rich instead of degrading to a generic label
- Added regressions for the exact recovery behaviors users notice:
  - boot emits `session.started`
  - read-path calls recreate presence when the daemon-side session row is missing
  - recall refresh on an already-live session does not spam duplicate `session` events
- Validation:
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`255` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
  - `4f1c5ea`
- Added a real `v0.4.1` -> current upgrade regression on the daemon boot path:
  - creates an on-disk legacy fixture using the historical `v0.4.1` schema
  - boots current code through `state::initialize`
  - runs the tracked migration ledger to latest schema version
  - asserts integrity, data preservation, token creation, FTS seed bookkeeping, and latest-schema columns/tables
- Closed the earlier recall explainability defer by adding a direct regression against the final policy JSON payload:
  - explain output now has deterministic coverage for crystal-family compaction reporting
  - compacted siblings are explicitly verified to remain outside generic post-budget `droppedCandidates`
- Cleared daemon clippy blockers in touched files so the release batch ends with both tests and lint green.
- Validation:
  - `rtk cargo test --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml` (`252` tests passing)
  - `rtk cargo clippy --manifest-path C:\\Users\\aditya\\cortex\\daemon-rs\\Cargo.toml -- -D warnings`
- Commit:
- `7beef8a`
- `4f1c5ea`
- `3c949e5`
- Promoted crystal recall hits into family heads instead of leaving them as parallel duplicate results:
  - raw member hits now collapse under the crystal result
  - recall payloads now expose `familyMembers`, `familySize`, and `collapsedSources`
  - crystal unfold returns consolidated text plus visible family-member source keys
- Hardened crystal compatibility for upgrade paths:
  - crystal search works even when older `memory_clusters` rows are missing ACL columns
  - crystal member expansion works across legacy memory/decision schemas
  - null-sourced memories now canonicalize to `memory::{id}` so they still collapse into the right family
- Preserved non-lossy recall behavior under served-content dedup by falling back to a collapsed child source when the crystal summary was already served.
- Tightened crystal family-head quality and durability:
  - crystal excerpts are now query-focused instead of raw leading slices
  - ranked collapsed-child fallback prefers the strongest unseen family member instead of lexical source order
  - cached recall payloads preserve ranked family-collapse metadata with backward-compatible deserialization
- Added pre-budget crystal-family compaction for low/medium token recall so one family does not spend multiple slots before truncation and selection.
- Added truthful family-compaction diagnostics in budget recall explain output:
  - trace now records candidate count before compaction and explicit family-compaction winners/losers
  - policy explain separates family-compaction drops from later budget/rank drops
- Added regression coverage for crystal unfold, crystal-family collapse, null-source family-member collapse, ranked child fallback selection, query-focused family-head excerpts, and cache roundtrip preservation.
- Added regression coverage for tight-budget family compaction before truncation.
- Added regression coverage for associative merge followed by crystal-family compaction in the budget trace.
- Validation:
  - `cargo fmt --all` (`daemon-rs`)
  - `cargo test --tests` (`daemon-rs`, 250 tests passing)
 - Commits:
   - `5d9ea40`
   - `cc97ec6`
   - `f348870`
   - `1fb0ca4`
   - `e5251dd`

## Since v0.4.1 - Major Tracks

### Daemon Lifecycle and One-Daemon Enforcement
- Shifted startup ownership toward service-first lifecycle and away from ad-hoc local spawns.
- Added readiness-first probing and canonical runtime identity checks across daemon, plugin, and Control Center.
- Hardened ownership claims, parent linkage, and respawn policy boundaries.
- Removed Control Center sidecar fallback and deleted legacy sidecar module.
- Removed MCP proxy respawn/shutdown lifecycle ownership branches and deleted dormant daemon spawn/respawn internals.
- Added strict spawn-path auditing to prevent lifecycle regressions.
- Added runtime startup denylist checks in daemon and app launch paths to block temp/runtime wrapper binaries before execution.

### IPC Foundation (Step 3 Kickoff)
- Added canonical local IPC endpoint resolution to runtime path model.
- Added optional daemon-side IPC listeners (Windows named pipe + Unix socket) in parallel with existing HTTP/TLS listeners.
- Surfaced IPC endpoint metadata in health/readiness runtime payloads.
- Centralized local endpoint resolution logic in a shared transport module to prepare client-side IPC preference rollout.
- Migrated MCP proxy local request paths to IPC-first with HTTP fallback (health checks, session APIs, and RPC loop).
- Added dedicated parser tests for URL/path splitting and IPC HTTP response handling.
- Migrated remaining local CLI/hook/admin health/boot client paths to shared IPC-first transport with HTTP fallback.
- Preserved remote target behavior by keeping explicit HTTP(S) routing for non-local base URLs.

### Plugin and MCP Routing Hardening
- Enforced attach-only plugin routing by default.
- Removed SessionStart daemon spawn behavior; hook is status/probe only.
- Added app-first/canonical binary resolution for plugin bridge and SessionStart probes.
- Dropped legacy routing/env aliases that conflicted with strict lifecycle behavior.
- Added local attach safety gate that blocks temporary/bundled plugin binaries by default, with explicit opt-in override.
- Added canonical promotion flow for plugin bundled binaries in local mode (`~/.cortex/bin`) so plugin-only users can avoid temp execution paths.

### Recall / Retrieval / Quality
- Added and refined precision ranking, relevance-band compaction, source prefix scoping.
- Added query isolation and retrieval quality hardening.
- Improved low-token recall quality and ranking consistency.
- Added trust-aware ranking blend (`score + trust_score`) across memory/decision keyword and semantic recall paths.
- Added mixed-scale importance normalization compatibility (`0-1` and legacy `0-100`).
- Added co-occurrence-driven associative expansion in budget recall to surface related sources from the same memory family while preserving strict low-budget guardrails.
- Promoted crystal retrieval into a true family-aware recall layer, with collapsed member hits and canonical crystal unfold behavior.
- Hardened crystal recall against older upgraded schemas and null-source family members.

### Agent Intelligence Telemetry (Step 9.3 Foundations)
- Added recall policy explainability surfaces for deterministic introspection of retrieval mode, ranking factors, and budget decisions.
- Added owner-scoped `agent_feedback` telemetry schema and migration path.
- Added MCP write/read telemetry tools:
  - `cortex_agent_feedback_record`
  - `cortex_agent_feedback_stats`
- Added HTTP telemetry endpoints:
  - `POST /agent-feedback`
  - `GET /agent-feedback/stats`
- Added reliability rollups by agent/task class with memory-source coverage metrics.
- Added adaptive recall depth tuning (`k`) from recent owner-scoped agent outcome telemetry.

### Recall Analytics + Stats Surface
- Added daemon `/stats` endpoint for recall telemetry rollups.
- Added tier distribution, mode mix, average latency, and aggregate budget spend/savings reporting.
- Added richer `recall_query` event payload normalization to support reliable stats aggregation.

### Plugin Local Mode and Cross-Platform Lifecycle
- Kept app-first architecture while enabling plugin-only local mode safely.
- Added service-first ensure semantics for plugin local mode on Windows.
- Added non-Windows plugin local ensure path with ownership-token linkage and parent identity controls.
- Updated plugin routing docs and behavior to prefer app-managed binaries while allowing safe local fallback.

### Storage Pressure Governance
- Added pressure-aware compaction governor with soft/hard DB thresholds.
- Added health payload fields for pressure state and utilization (`db_pressure`, limits, utilization).
- Moved compaction scheduling toward periodic pressure checks instead of unconditional background full passes.
- Added explicit threshold tests for governor activation criteria to prevent drift in compaction trigger policy.
- Goal: keep local DB bounded and cheap for recall usage without introducing hard write failures.

### Security / Auth / Boundary Controls
- Hardened daemon identity verification before token fallback.
- Tightened auth retry behavior and fail-closed team-mode MCP checks.
- Strengthened SSRF and request-boundary handling.

### Client Policy / Access Controls
- Added `client_permissions` schema and migration foundation for per-client/per-tool MCP policy.
- Added read/write/admin permission gates in MCP tool dispatch.
- Added MCP admin tools for permission grant/revoke/list management.
- Added provenance/trust schema + write path foundation (`source_client`, `source_model`, `reasoning_depth`, `trust_score`).
- Preserved backward compatibility with permissive behavior until explicit policy rows are configured.
- Added HTTP permission admin surfaces and Control Center permission management UI.

### Conflict Detection / Resolution Intelligence
- Added first-class conflict classification (`AGREES`, `CONTRADICTS`, `REFINES`, `UNRELATED`) in daemon conflict analysis.
- Added durable conflict records via `decision_conflicts` schema + migration.
- Added trust-aware policy handling during store writes (auto-resolve/merge/supersede/open conflict branching).
- Added admin conflict MCP tools (`cortex_conflicts_list`, `cortex_conflicts_get`, `cortex_conflicts_resolve`).
- Upgraded Control Center conflicts UI with classification/status/timestamp/trust context and richer manual resolution controls.

### Reliability / Storage / Operations
- Startup integrity checks, WAL/checkpoint/backup hardening, retention/cleanup passes.
- Added health storage metrics, cleanup CLI, TTL cleanup support, and operational safeguards.
- Improved control-center restart/recovery behavior and reduced UI instability.

### Tooling / CI / Benchmarks / Docs
- Added strict lifecycle audits, clippy/expect gates, benchmark workspace/adapter hardening.
- Significant README/research/tracker revisions during v0.5.0 iteration.
- Added cross-platform plugin routing dry-run validation in CI (Linux/macOS/Windows) to catch platform drift in plugin entrypoint behavior.

## Updating This File
1. Add a dated summary entry for each meaningful batch.
2. Regenerate the raw ledger section from v0.4.1..HEAD.
3. Keep release-note wording in updates-to-readme.md aligned.
4. Include both `0.5.0`-prefixed and non-prefixed commits in each sweep (do not filter by prefix).

## Raw Commit Ledger (v0.4.1..HEAD)
Format: YYYY-MM-DD | <short_sha> | <subject>

- 2026-04-15 | d63257d | feat(stats): add vec0 shadow routing gate diagnostics
- 2026-04-15 | 52739fe | feat(stats): add shadow semantic recall rollups
- 2026-04-15 | 3654260 | feat(recall): add shadow semantic telemetry to unified recall
- 2026-04-15 | deeecb3 | fix(recall): classify missing sqlite-vec as shadow unavailable
- 2026-04-15 | f938ea0 | fix(recall): preserve semantic candidates on solo schema
- 2026-04-15 | 06088fd | feat(recall): add sqlite-vec shadow knn explain diagnostics
- 2026-04-15 | 39c32a2 | test(benchmark): add distilled proxy-vs-full ranking regression
- 2026-04-15 | f7f3c3d | feat(db): bootstrap sqlite-vec groundwork
- 2026-04-15 | d26657a | feat(recall): align synonym term groups across ranking
- 2026-04-15 | 200fbef | feat(recall): add query-adaptive weighted fusion
- 2026-04-15 | aebefce | fix(lifecycle): close restart verification gaps
- 2026-04-15 | 7beef8a | test(release): harden upgrade and recall explain regressions
- 2026-04-15 | 8b9eea2 | Update README.md
- 2026-04-15 | 165f765 | Update README.md
- 2026-04-15 | f50d51e | Update README.md
- 2026-04-15 | 0f58482 | feat(recall): add associative co-occurrence expansion
- 2026-04-15 | 80019f5 | harden daemon binary path guards across app and plugin
- 2026-04-15 | 56e6169 | test(storage): lock governor threshold behavior
- 2026-04-15 | c8ace09 | ci: validate plugin routing dry-run across Linux/macOS/Windows
- 2026-04-15 | 005c6c9 | Enable cross-platform plugin local daemon ensure
- 2026-04-15 | d1ec974 | Allow safe plugin-only local mode with app-first routing
- 2026-04-15 | 62db111 | Harden plugin lifecycle and add storage pressure governor
- 2026-04-14 | 1b39357 | 0.5.0 - feat: add recall stats endpoint and tier telemetry
- 2026-04-14 | 78f98b5 | feat(intelligence): adapt recall depth from agent feedback
- 2026-04-14 | 8523bcb | feat(intelligence): add agent outcome telemetry APIs and MCP tools
- 2026-04-14 | 85b28dc | feat(recall): add recall policy explain endpoint and MCP tool
- 2026-04-14 | 4725de6 | feat(benchmark): add baseline auto-tightening non-regression gates
- 2026-04-14 | b09a51c | feat(benchmark): add dynamic provider-aware token gating
- 2026-04-14 | 2538d43 | feat(transport): use shared IPC-first client routing for boot, hook, admin, and health probes
- 2026-04-14 | ed73143 | feat(mcp): route local proxy traffic over IPC with HTTP fallback
- 2026-04-14 | 04e869d | refactor(transport): centralize local daemon endpoint resolution
- 2026-04-14 | 223686f | feat(ipc): add local IPC listener foundation and harden admin surfaces
- 2026-04-14 | 3ba3544 | feat(permissions): expose client policy controls in daemon and control center
- 2026-04-14 | dd5bbd0 | feat(conflicts): ship classified conflict engine, admin tooling, and UI
- 2026-04-14 | 34b72b9 | chore(plugin): drop legacy local-spawn flag references
- 2026-04-14 | 0cc0082 | feat(plugin): block temp bundled binaries in local attach mode
- 2026-04-14 | 7e1dd40 | feat(recall): blend trust score into ranking and semantic recall
- 2026-04-14 | 6116a9b | feat(provenance): persist trust metadata on memory writes
- 2026-04-14 | 7374f31 | feat(mcp): add permission grant/list/revoke admin tools
- 2026-04-14 | c02240c | chore(auth): remove unused control-center lock helpers
- 2026-04-14 | e0dd421 | feat(mcp): add client permission schema and per-tool access gates
- 2026-04-14 | 7989d82 | refactor(lifecycle): remove proxy respawn paths and dead spawn machinery
- 2026-04-14 | df5c367 | refactor(lifecycle): remove dormant mcp auto-spawn branches
- 2026-04-14 | 01ae94b | test(lifecycle): enforce no-sidecar regressions in spawn audit
- 2026-04-14 | 01da61f | chore(plugin): drop legacy CORTEX_DEV_APP_URL route alias
- 2026-04-14 | b6a5d5b | feat(lifecycle): enforce service-only startup and app-first plugin binary routing
- 2026-04-14 | 56978ca | feat(daemon): make readiness bind-accurate and harden service ensure probes
- 2026-04-14 | 6e17dca | feat(plugin): make SessionStart readiness-aware with health fallback
- 2026-04-14 | 16cb6ae | feat(control-center): probe readiness before health for daemon reachability
- 2026-04-14 | 94c5475 | feat(daemon): add readiness endpoint and readiness-first lifecycle probes
- 2026-04-14 | 6d40e79 | feat(control-center): log deterministic startup path decisions
- 2026-04-14 | f9f2c1a | feat(control-center): require service-first startup unless fallback is explicit
- 2026-04-14 | 2e186ca | feat(plugin): enforce attach-only routing and remove local spawn paths
- 2026-04-14 | 9dee06b | feat(startup): add service ensure and service-first app boot
- 2026-04-14 | 09cef02 | fix(plugin): force canonical local home and assert no-autospawn
- 2026-04-14 | de81771 | fix(plugin): default to attach-only and require spawn opt-in
- 2026-04-14 | d496bbe | fix(spawn): use stable source-relative runtime copy path
- 2026-04-14 | ead0f73 | fix(startup): preflight single-daemon identity before boot
- 2026-04-14 | 4216dc1 | fix(lifecycle): bind spawned owner claims to parent process start time
- 2026-04-14 | 9bd7f7a | fix(daemon): enforce signed owner claims at startup
- 2026-04-14 | 3a62bb1 | feat(daemon): tighten ownership policy and low-token recall quality
- 2026-04-14 | 77f73e6 | fix(plugin): eliminate daemon spawn from SessionStart hook and harden MCP bridge
- 2026-04-14 | b68a29c | ci(hardening): enforce daemon spawn path audit
- 2026-04-14 | 7ed6927 | feat(lifecycle): issue signed owner claims for daemon spawns
- 2026-04-14 | 652fb76 | refactor(lifecycle): centralize respawn ownership policy
- 2026-04-14 | cf86f74 | chore(hardening): add strict daemon spawn path auditor
- 2026-04-14 | 09598c4 | fix(auth): harden control-center lock detection
- 2026-04-14 | b1717e2 | fix(daemon): require parent linkage for spawned owner daemons
- 2026-04-14 | 592111d | chore(build): scope windows-sys to test-only dependencies
- 2026-04-14 | 9edb93b | test(mcp): clean up daemon child processes on Windows failures
- 2026-04-14 | 1b1291d | fix(daemon): enforce spawned-daemon parent ownership lifecycle
- 2026-04-14 | 8792d24 | feat(recall): add daemon-side source prefix scoping
- 2026-04-14 | b3295a2 | feat(recall): compact budgeted results by relevance bands
- 2026-04-14 | dd1131e | feat(recall): tighten precision ranking and query-aware feedback
- 2026-04-14 | 92876b8 | fix: require verified local daemon identity before boot token fallback
- 2026-04-14 | 3519539 | test: block local mcp spawn when control-center lock is active
- 2026-04-14 | 82a47f6 | test: block local token leakage to wrong-instance listeners
- 2026-04-14 | 2033a68 | fix: gate plugin local spawn and validate daemon identity
- 2026-04-14 | 2013c0a | fix: harden control-center daemon identity checks
- 2026-04-14 | d68de2f | fix: enforce single-owner daemon auth and liveness gates
- 2026-04-14 | f497063 | chore: checkpoint verified local daemon-rs changes
- 2026-04-13 | f1871d2 | Harden daemon respawn coordination and MCP target validation
- 2026-04-13 | 45feb62 | fix: harden mcp auth path and secure fallback behavior
- 2026-04-13 | 876735e | gitignore: exclude local startup-script and graphify artifacts
- 2026-04-13 | 9c93ea5 | daemon-rs: harden bind-aware daemon targeting and remote mcp flows
- 2026-04-13 | 5fc9ca8 | harden mcp auth boundaries and restart reliability
- 2026-04-13 | 01b1a77 | default windows service install to manual start
- 2026-04-13 | 59f55f0 | harden unfold_source for legacy schemas and add coverage
- 2026-04-13 | 7fa4380 | daemon_mcp_proxy_normalize_api_key_and_source_headers
- 2026-04-13 | a253795 | daemon_mcp_only_manage_lifecycle_for_owned_daemon
- 2026-04-13 | 2e48637 | "daemon_fail_closed_team_mode_mcp_dispatch_without_caller"
- 2026-04-13 | 9c081aa | "daemon_mcp_rpc_preserve_http_auth_status_with_jsonrpc_errors"
- 2026-04-13 | ef885f0 | "daemon_harden_health_identity_checks_and_duplicate_serve_exit"
- 2026-04-13 | 9f919a4 | "daemon_harden_respawn_coordination_and_proxy_base_sync"
- 2026-04-13 | 3b3d263 | "daemon_harden_team_mcp_auth_scope_and_boot_identity"
- 2026-04-13 | e3ce57e | daemon_mcp_proxy_refresh_urls_after_respawn_and_heartbeat_recovery
- 2026-04-13 | 9516648 | daemon_mcp_boot_scope_isolation_and_fail_closed_rpc_auth
- 2026-04-13 | 668d19e | daemon_ssrf_error_now_includes_header_integration_hint
- 2026-04-13 | 3470990 | chore_ignore_local_graphify_artifacts
- 2026-04-13 | 9b676f8 | daemon_setup_add_codex_toml_merge_and_idempotent_config_updates
- 2026-04-13 | 42d836c | daemon_db_atomic_migration_apply_and_record
- 2026-04-13 | 9daa82d | daemon_conductor_owner_scoping_and_lock_race_hardening
- 2026-04-13 | 4637496 | daemon_recall_scope_and_boot_unfold_hardening
- 2026-04-13 | eed716e | docs_use_cortex_boot_for_cli_onboarding
- 2026-04-13 | 4a9c481 | daemon_boot_cli_and_mcp_resilience
- 2026-04-13 | f05d5f3 | Clean up orphaned MCP wrapper sessions
- 2026-04-13 | c41ce8b | Verify desktop config paths across platforms
- 2026-04-13 | 24a64cc | Harden MCP daemon startup and auth wiring
- 2026-04-13 | e4f7f8d | Lazy-load the brain visualizer
- 2026-04-13 | 99f2620 | Prevent duplicate control-center instances
- 2026-04-13 | 8d92cab | Use persisted graph links in brain visualizer
- 2026-04-13 | 3e5421a | Fix recall deadlock and solo last-call lookup
- 2026-04-12 | c25ba7f | docs: update v050 tracker
- 2026-04-12 | dffeb8d | chore: harden isolated benchmark runner
- 2026-04-12 | e210ffb | chore: add isolated cortex benchmark adapter
- 2026-04-12 | 92212b5 | chore: add benchmark workspace bootstrap
- 2026-04-12 | 670551e | 0.5.0 - docs: update v050 tracker
- 2026-04-12 | 7f2b7ce | 0.5.0 - fix: scope recall dedup per query
- 2026-04-12 | 435ee35 | 0.5.0 - docs: update v050 tracker
- 2026-04-12 | 066cc62 | 0.5.0 - chore: isolate daemon housekeeping cleanup
- 2026-04-12 | 8ac2064 | 0.5.0 - chore: wire expect smoke ci
- 2026-04-12 | af12436 | 0.5.0 - docs: update v050 tracker
- 2026-04-12 | 0d87651 | 0.5.0 - fix: enforce single-owner cli daemon flows
- 2026-04-12 | 82d4d0d | 0.5.0 - docs: update v050 tracker
- 2026-04-12 | d57aaf3 | 0.5.0 - fix: harden daemon shutdown auth retry
- 2026-04-12 | a1405bc | 0.5.0 - docs: update v050 tracker
- 2026-04-12 | a463994 | 0.5.0 - fix: tighten daemon exit ownership
- 2026-04-11 | 628f15b | 0.5.0 - fix: remove panel stutter
- 2026-04-11 | 9abc9d7 | 0.5.0 - chore: stage benchmark workspace
- 2026-04-11 | e70e67e | 0.5.0 - feat: expand mcp setup wizard
- 2026-04-11 | 5c6d5bd | 0.5.0 - docs: track single-owner daemon enforcement
- 2026-04-11 | 1f02bd0 | 0.5.0 - fix: enforce single-daemon mcp ownership
- 2026-04-11 | 30f760d | 0.5.0 - docs: track analytics and MCP surface polish
- 2026-04-11 | d29736e | 0.5.0 - fix: smooth analytics transitions and MCP setup feedback
- 2026-04-11 | 099fa3c | 0.5.0 - docs: track control center polish passes
- 2026-04-11 | 69344ca | 0.5.0 - fix: clarify brain agent node colors
- 2026-04-11 | 090baef | 0.5.0 - fix: smooth control center panel recovery
- 2026-04-11 | 3d5345b | 0.5.0 - docs: track runtime ownership hardening
- 2026-04-11 | 0211aac | 0.5.0 - fix: own daemon runtime and stable mcp paths
- 2026-04-11 | 67335e2 | 0.5.0 - docs: update v050 tracker
- 2026-04-11 | 2840688 | 0.5.0 - fix: smooth control center recovery and version truth
- 2026-04-11 | 494093b | 0.5.0 - docs: update v050 tracker
- 2026-04-11 | 6930a2c | 0.5.0 - fix: harden control center lifecycle recovery
- 2026-04-11 | 8c20b71 | 0.5.0 - docs: update v050 tracker
- 2026-04-11 | 7cca348 | 0.5.0 - fix: stabilize control center live surfaces
- 2026-04-11 | 5567cec | 0.5.0 - fix: scope daemon auth to runtime paths
- 2026-04-11 | ca47aa1 | docs: record phase 7l runtime copy isolation
- 2026-04-11 | ef77822 | fix: isolate debug runtime daemon copies
- 2026-04-11 | aaa3d0a | docs: record phase 7k runtime copy launcher
- 2026-04-11 | b02cd31 | fix: spawn dev daemon from runtime copy
- 2026-04-11 | 7def040 | docs: record phase 7j dev watcher cleanup
- 2026-04-11 | 0245aed | fix: quiet desktop dev rebuild noise
- 2026-04-11 | 30c387b | chore: ignore isolated daemon build targets
- 2026-04-11 | c50dfd7 | docs: record phase 7i build target isolation
- 2026-04-11 | eebcca2 | fix: isolate desktop daemon build targets
- 2026-04-11 | ddb7fce | docs: record phase 7h desktop auth cleanup
- 2026-04-11 | 4d108a6 | fix: clear stale desktop auth banners
- 2026-04-11 | b5ff92e | docs: record phase 7g lifecycle hardening
- 2026-04-11 | 0b0b0fc | fix: harden daemon lifecycle and desktop auth flows
- 2026-04-11 | 95b1b1e | docs: record duplicate serve auth fix in v050 tracker
- 2026-04-11 | 286d781 | fix: prevent duplicate serve from rotating auth token
- 2026-04-10 | ead685e | 0.5.0 - feat: add quality-gated semantic dedup
- 2026-04-10 | 4b617a3 | 0.5.0 - feat: add ttl expiration cleanup
- 2026-04-10 | 8b6ed2c | 0.5.0 - ci: add clippy all-targets gate
- 2026-04-10 | fdd3f25 | 0.5.0 - chore: verify clippy all-targets clean
- 2026-04-10 | 1cb0db5 | 0.5.0 - feat: add health storage metrics
- 2026-04-10 | 5fbfb39 | 0.5.0 - feat: add cleanup cli
- 2026-04-10 | c4d35e1 | 0.5.0 - feat: cleanup stale pid files
- 2026-04-10 | 513b708 | 0.5.0 - feat: compact write buffer after replay
- 2026-04-10 | 2a619f5 | 0.5.0 - feat: startup log rotation
- 2026-04-10 | cb3459d | 0.5.0 - feat: bridge-backup cleanup
- 2026-04-10 | b4878f0 | 0.5.0 - feat: backup retention policy
- 2026-04-10 | 80db831 | 0.5.0 - docs: record README support link restoration
- 2026-04-10 | ff58675 | 0.5.0 - docs: restore README support link
- 2026-04-10 | 47d41f0 | 0.5.0 - docs: record README hero simplification
- 2026-04-10 | fd3cc82 | 0.5.0 - docs: simplify README hero composition
- 2026-04-10 | c2b2f17 | 0.5.0 - docs: record README stack compatibility clarification
- 2026-04-10 | 07a2c81 | 0.5.0 - docs: clarify README stack compatibility
- 2026-04-10 | d71c7b2 | 0.5.0 - docs: record README monte carlo redesign
- 2026-04-10 | be3c5fa | 0.5.0 - docs: rebuild README monte carlo proof surface
- 2026-04-10 | 6220e05 | 0.5.0 - docs: record README copy simplification
- 2026-04-10 | 69b3f32 | 0.5.0 - docs: simplify README copy and pacing
- 2026-04-10 | 82cf37a | 0.5.0 - docs: record README section header removal
- 2026-04-10 | 40f04dd | 0.5.0 - docs: remove README section header bars
- 2026-04-10 | 46ce67e | 0.5.0 - docs: record README section header bars
- 2026-04-10 | eac54d0 | 0.5.0 - docs: add README section header bars
- 2026-04-10 | 7f127cc | 0.5.0 - docs: record README card readability pass
- 2026-04-10 | fcc9be3 | 0.5.0 - docs: enlarge README card copy
- 2026-04-10 | 7c730b1 | 0.5.0 - docs: record README middle layout pass
- 2026-04-10 | 9b7df79 | 0.5.0 - docs: refine README middle section layout
- 2026-04-10 | 03ad982 | 0.5.0 - docs: record README proof simplification
- 2026-04-10 | 904c35c | 0.5.0 - docs: simplify README proof surfaces
- 2026-04-10 | 07bfd97 | 0.5.0 - docs: record README hero minimalism pass
- 2026-04-10 | 0b98ccd | 0.5.0 - docs: simplify README hero presentation
- 2026-04-10 | aff7552 | 0.5.0 - docs: record README hero proof polish
- 2026-04-10 | 8ec93c0 | 0.5.0 - docs: polish README hero proof surfaces
- 2026-04-10 | 56fe60a | 0.5.0 - docs: record README art direction pass
- 2026-04-10 | d729b98 | 0.5.0 - docs: refine README hierarchy and pacing
- 2026-04-10 | 401c42d | 0.5.0 - docs: record README render correction
- 2026-04-10 | ea151df | 0.5.0 - docs: fix README GitHub rendering
- 2026-04-10 | 54f1abf | 0.5.0 - docs: record README real visual rewrite
- 2026-04-10 | edd39a3 | 0.5.0 - docs: rebuild README around real product proof
- 2026-04-10 | 786c926 | 0.5.0 - docs: record README landing page polish
- 2026-04-10 | 43a0bd8 | 0.5.0 - docs: refine README landing page presentation
- 2026-04-10 | 6a43750 | 0.5.0 - docs: record README monte carlo surface
- 2026-04-10 | 632da53 | 0.5.0 - docs: add monte carlo proof surface to README
- 2026-04-10 | 5fb1351 | 0.5.0 - docs: record analytics redesign in tracker
- 2026-04-10 | aedf364 | 0.5.0 - ui: redesign analytics visual system
- 2026-04-10 | b278aad | 0.5.0 - docs: record research visual refinement
- 2026-04-10 | 501be51 | 0.5.0 - docs: refine research visual language
- 2026-04-10 | c7f6fcb | 0.5.0 - docs: record README and research redesign
- 2026-04-10 | 8a6fdcc | 0.5.0 - docs: redesign README and deepen research record
- 2026-04-10 | 2d1ea5e | 0.5.0 - docs: expand research page and clarify influences
- 2026-04-10 | db5e986 | 0.5.0 - docs: surface research in README navigation
- 2026-04-10 | e24a0ec | 0.5.0 - docs: add public research page and README research section
- 2026-04-10 | cebbfb9 | docs: record review fixes for feed and auth retry
- 2026-04-10 | 261574e | fix: harden feed replay and desktop token retry
- 2026-04-10 | 506ee25 | docs: record cortex help troubleshooting entrypoint
- 2026-04-10 | 76f305d | docs: add cortex help troubleshooting guidance
- 2026-04-10 | bc1827e | docs: record cortex connectivity hardening
- 2026-04-10 | 9d9b318 | fix: harden cortex connectivity and auth docs
- 2026-04-09 | c06aca8 | refactor: clean up repo root -- consolidate security docs, remove stale artifacts
- 2026-04-08 | 32ea351 | fix: remove internal v050 plan from tracking (should be gitignored)
- 2026-04-08 | 5f85169 | feat: add Phase 5E storage compression to v050 plan (ASAP priority)
- 2026-04-08 | 2da0dfc | fix: install to Program Files instead of AppData for discoverability
- 2026-04-08 | 1abf027 | fix: auto-retry with token re-read on 401 during startup race condition
- 2026-04-08 | 23490fb | fix: dedupe agent sessions + remove tray close button from control center
- 2026-04-08 | 3fbb861 | refactor: rustfmt cleanup + indexer improvements across daemon
- 2026-04-08 | 0551baa | fix: stabilize restart transitions and boot agent visibility
- 2026-04-08 | 5dea8a3 | feat: upgrade control center restart and analytics UI
- 2026-04-08 | faca46e | feat: add operation-level savings telemetry
- 2026-04-08 | 0d79361 | 0.5.0 - feat: add DB size monitoring
- 2026-04-08 | 2292a43 | feat: add Claude Code and Factory Droid as contributors
- 2026-04-08 | dfb6302 | 0.5.0 - feat: add TTL expiration support
- 2026-04-08 | d56f97f | Merge branch 'feat/v050-phase-3b-ttl'
- 2026-04-08 | d5d58cd | fix: prevent control center hangs on daemon start/stop
- 2026-04-08 | 7081dc1 | 0.5.0 - feat: harden reconnect flow and agent/session telemetry
- 2026-04-08 | 4d019fb | Merge pull request #6 from AdityaVG13/feat/v050-phase-3a-schema
- 2026-04-08 | 145766b | 0.5.0 - feat: add schema versioning and cortex doctor command
- 2026-04-07 | 6c7698a | chore: untrack internal v050 docs (already in .gitignore)
- 2026-04-07 | ba99fb3 | fix: sidecar resolves stale debug binary, add debug path fallback
- 2026-04-07 | b5247b1 | fix: daemon lifecycle bugs + savings calculation
- 2026-04-07 | c14374b | Merge pull request #5 from AdityaVG13/feat/v050-phase-1-retrieval
- 2026-04-07 | ad74a92 | 0.5.0 - feat: wire tiered retrieval pipeline with RRF fusion and compound scoring (task 1.5)
- 2026-04-07 | 082f275 | 0.5.0 - feat: add compound scoring with recency and importance (task 1.4)
- 2026-04-07 | ed1d3a7 | 0.5.0 - docs: add Phase 1 completion report for tasks 1.1-1.3
- 2026-04-07 | 33def16 | 0.5.0 - feat: add RRF fusion function with unit tests (task 1.3)
- 2026-04-07 | 980f66b | 0.5.0 - feat: crash-safe WAL (10s checkpoint + startup recovery) and rolling backups
- 2026-04-07 | 3576c5c | 0.5.0 - feat: startup integrity gate and periodic quick_check
- 2026-04-07 | a82d747 | 0.5.0 - feat: add auto_repair() with dump-and-rebuild and quick_check()
- 2026-04-07 | a6e5d9d | 0.5.0 - fix: replace filesystem baseline with DB query for agent-agnostic savings
- 2026-04-06 | eeda13f | 0.5.0 - chore: purge 29 duplicate memories, re-run recall baseline
- 2026-04-06 | 6bdf63e | 0.5.0 - chore: snapshot v0.4.1 recall baseline before v0.5.0 changes
- 2026-04-06 | 41daeec | docs: add local-first privacy headline to README
- 2026-04-06 | bde9f96 | 0.5.0 - fix: wire agent sessions so Agents panel shows online agents
- 2026-04-06 | 626ea39 | docs: add desktop app known limitation to README
- 2026-04-06 | f0ea19c | fix: regenerate SHA256SUMS after flattening desktop artifacts
- 2026-04-06 | da0505c | chore: remove auto-changelog workflow
- 2026-04-06 | 20530f0 | fix: flatten desktop artifacts before release upload
- 2026-04-16 00:33:10 -04:00 | c5ffa4 | Small task: hardened benchmark singleton-skip detection and env scrub in daemon-rs/tests/recall_benchmark.rs.
- 2026-04-16 00:33:24 -04:00 | 1ad69b3 | Small task: added allow_service_ensure short-circuit coverage for local spawn policy in daemon-rs/src/main.rs.
- 2026-04-16 00:33:48 -04:00 | d3d7c37 | Small task: added regression test asserting shadow gate holds on high rank-delta / low top1 match in daemon-rs/src/handlers/health.rs.
- 2026-04-16 01:18:36 -04:00 | 4cb8941 | Phase-2A: normalized shadow status buckets, added per-metric sample sufficiency blockers, and expanded shadow gate regressions in daemon-rs/src/handlers/health.rs.
- 2026-04-16 01:19:42 -04:00 | ebcf704 | Benchmark: tightened distilled proxy fidelity gates (pairwise + coverage), widened explain sample breadth, and enforced CI-fatal singleton skip handling in daemon-rs/tests/recall_benchmark.rs.
- 2026-04-16 01:21:54 -04:00 | 96c7942 | Test reliability: serialized daemon-spawn MCP integration tests and aligned rejection assertions with APP_INIT_REQUIRED attach-only behavior.
- 2026-04-16 01:42:38 -04:00 | 335ea61 | Phase-2A retrieval quality: migrated FTS tokenizer to porter unicode61 via schema migration 012, aligned fresh schema tokenizer, and added migration + tokenizer invariants in db/state tests.
- 2026-04-16 01:44:50 -04:00 | c3b6300 | Recall tuning: introduced explicit BM25 constants/parameterized SQL and added top-k ranking regressions proving text/decision-body signals beat metadata-only matches.
- 2026-04-16 01:49:01 -04:00 | 854165e | Embeddings: added all-MiniLM-L12-v2 profile option + aliases, bounded periodic backfill passes, queue-limit regression coverage, and setup-time backlog observability.
- 2026-04-16 01:53:04 -04:00 | 67553c9 | OpenAPI: bumped spec to 0.5.0, expanded health/readiness/recall-explain/stats contracts, and added test guards to prevent future spec/runtime drift.

- 2026-04-16 | ebab223 | test: add Python/TypeScript SDK conformance suites and CI gates
- 2026-04-16 | 025a91d | feat: add guarded sqlite-vec canary routing with deterministic sampling, strict shadow gates, and semantic-route telemetry

- 2026-04-16 03:53:31 -04:00 | 94c90d7 | Prompt injector hardening: strict CLI value validation, true <file>.injected output suffixing, URL-safe boot query construction, higher-resolution watch change detection, and non-global token-reader test coverage.

- 2026-04-16 04:20:50 -04:00 | fd49244 | Reliability + portability guards: added health runtime-path home-scope integration coverage, concurrent attach-only app-client policy regression, and removed developer-specific daemon fixture paths from source tests.

- 2026-04-16 04:52:11 -04:00 | f60a2d1 | Hardened scripts/clean_install_smoke.sh for out-of-box reliability: LF-safe bash bootstrap, cargo.exe fallback on Windows bash, fail-closed search error handling, graphify/target scan exclusions, ASCII status output, and optional README enforcement via CORTEX_ENFORCE_PUBLIC_README.
- 2026-04-16 05:31:27 -04:00 | 77c175d | One-daemon hardening: improved control-center lock contention detection (including Windows lock codes 32/33), enforced lock-aware attach-only gating in ensure_daemon, and added debug/test-only single-daemon bypass path for daemon integration suites; validated with full daemon test/clippy/benchmark/audit stack.

## 2026-04-16 � fba87af
- Added enchmarking/adapters/tests/test_cortex_http_client.py with 5 contract tests covering headers, serialization, recall filtering/dedupe, metrics JSONL writing, and namespace reset semantics.

## 2026-04-16 � b236c21
- Added reusable multi-response test server helper in daemon unit tests and new startup preflight canonical/different-identity assertions.
- Hardened ensure_daemon with control-center lock snapshot checks to prevent race-induced local spawn attempts while the app lock is/was held.

## 2026-04-16 � 4b057ff
- scripts/release-smoke-test.sh now validates duplicate serve rejection (one-daemon invariant) and verifies paths --json stays scoped to the smoke home.
- scripts/clean_install_smoke.sh now requires Python, includes plugin startup leak checks, and enforces 	ools/audit_spawn_paths.py --strict as part of the gate.

## 2026-04-16 � a09a5db
- Added new admin handler tests validating unowned-row ownership backfill across default tables, strict table allowlist enforcement, and empty-ID visibility no-op semantics.
- [2026-04-16 12:23:16 -04:00] commit cceaef9 test(lifecycle): cover spawn-parent orphan watcher shutdown path
  - Extracted daemon orphan-watch loop into a reusable helper and added async regression coverage proving parent identity break triggers shutdown sender consumption.

- [2026-04-16 12:26:29 -04:00] commit ad9ba3c test(plugin): add route and attach-contract conformance coverage
  - Refactored run-mcp bridge into testable contract helpers, added node contract suite for route/args/env ownership behavior, and enforced that suite in CI plugin validation.

- [2026-04-16 12:29:50 -04:00] commit 01ef26b feat(recall): boost ranking with entity-alignment signals
  - Added entity-like term extraction/alignment boosts to unified + semantic recall ranking and surfaced entity match/overlap/boost factors in recall explain payloads with dedicated regressions.

- [2026-04-16 12:40:08 -04:00] commit 699c351 test(reliability): add cross-surface attach and spawn-audit regressions
  - Added cross-surface concurrent attach-only regression for shared-home CLI/plugin callers, made spawn audit root override + missing-path handling robust, and added strict audit pytest regressions wired into CI.

- [2026-04-16 13:06:41 -04:00] commit 35adfde feat(extension): add secure MV3 cortex chrome companion scaffold
  - Added a production-oriented Manifest V3 Chrome extension (background service worker, popup/options UI, context-menu store flow, runtime origin-permission gating, local-first defaults) plus policy-alignment notes and core unit tests.

- [2026-04-16 13:07:07 -04:00] commit 931536a feat(adapter): add openai function-call cortex adapter contract
  - Implemented OpenAI function adapter execution layer over Cortex HTTP (health, store, 
ecall) with strict argument validation and dedicated pytest contract coverage.

- [2026-04-16 13:07:34 -04:00] commit 300ca30 test(ci): enforce chrome extension and adapter contract suites
  - Added CI jobs for benchmark adapter pytest coverage and Chrome extension manifest/core tests to prevent adapter and extension contract drift before merge.

- [2026-04-16 13:18:58 -04:00] commit ede6634 test(lifecycle): verify orphan watcher against real parent exit
  - Added process-level regression proving spawn-parent orphan watcher observes a real parent-process exit and triggers daemon shutdown signaling, tightening one-daemon parent-death coverage.


## 2026-04-16 13:41 � 5eeb1ad
- Added daemon POST /recall support and wired extension recall traffic to body-based requests to avoid query-string leakage of recall prompts.
- Hardened Chrome extension Web Store posture: loopback-only host model, removed wildcard optional host permissions, session-default API key storage, and opt-in page metadata capture.
- Added extension privacy policy draft and refreshed policy-compliance references to official Chrome documentation.
- Validation: 
ode --test extensions/cortex-chrome-extension/tests/core.test.mjs; 
tk cargo fmt; 
tk cargo test; 
tk cargo clippy -- -D warnings; 
tk cargo test --test recall_benchmark -- --nocapture.

## 2026-04-16 13:46 � eb11ef0
- Added 	ools/validate_chrome_extension_policy.py to enforce MV3 Web Store guardrails (loopback-only hosts, no wildcard optional hosts, no remote scripts/eval patterns, required policy docs present).
- Wired the guardrail into .github/workflows/ci.yml under chrome-extension-validation.
- Validation: python tools/validate_chrome_extension_policy.py; 
ode --test extensions/cortex-chrome-extension/tests/core.test.mjs; python tools/audit_spawn_paths.py --strict.

## 2026-04-16 16:25 � 32f955b
- Synced release-facing docs for v0.5 closeout in tracked Info/ surfaces:
  - Info/roadmap.md now reflects v0.5 stabilization closeout status and remaining release-doc targets.
  - Info/team-mode-setup.md replaced insecure broad-bind examples with security-first deployment matrix.
  - Added Info/startup-matrix-troubleshooting.md as one-daemon startup truth + failure triage playbook.
  - Linked startup matrix from Info/connecting.md.
- Closeout impact: directly addresses unified-plan blockers for roadmap/release-facing sync and startup/troubleshooting refresh.

## 2026-04-16 16:26 � 80dab99
- Hardened tracked public security guidance for team-mode transport boundaries in Info/security-rules.md:
  - non-loopback binds now explicitly require TLS on public/routed interfaces
  - HTTP-only path explicitly limited to private encrypted mesh interfaces
  - deployment recommendations tightened to prevent accidental raw internet exposure
- Closeout impact: reinforces docs/security contract alignment for v0.5 release surfaces.

## 2026-04-16 18:04 � f525244
- Closed all 13 security findings raised in the external review sweep.
- Daemon hardening:
  - Enforced rated auth before admin checks on admin surfaces; destructive mutate endpoints now require admin in team mode.
  - Reordered SSRF guard before token verification and expanded rated-auth helper adoption on protected handlers.
  - Enforced caller-scoped team behavior (fail closed when caller identity is absent) for store/boot/recall/feed/feedback flows.
  - Owner-scoped conflict detection, semantic candidate selection, merge/supersede updates, and duplicate comparison pool in team mode.
- SDK/plugin/desktop/runtime hardening:
  - Python/TypeScript SDKs now require explicit token for non-loopback base URLs.
  - Plugin bridge no longer passes API key in argv; remote key is env-scoped only.
  - Hook boot health check supports auth header + readiness/health fallback.
  - Desktop Tauri UI now reflects local-only connection semantics; unmanaged external daemon shutdown semantics preserved.
  - Benchmark script no longer uses hardcoded developer path; extension local URL normalization aligned to loopback HTTP policy.
  - prepare-runtime archive extraction now uses arg-based spawnSync instead of interpolated shell command strings.
  - Tauri home resolution now falls back through dirs::home_dir().
- Validation:
  - rtk cargo fmt --manifest-path daemon-rs/Cargo.toml
  - rtk cargo test --manifest-path daemon-rs/Cargo.toml (345 passed)
  - rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings
  - rtk cargo test --manifest-path daemon-rs/Cargo.toml --test recall_benchmark -- --nocapture (7 passed)
  - node plugins/cortex-plugin/scripts/run-mcp.contract.test.cjs (10 passed)
  - npm test in sdks/typescript (4 passed)
  - python -m pytest in sdks/python (4 passed)
  - node --test extensions/cortex-chrome-extension/tests/*.test.mjs (9 passed)


## 2026-04-17 13:52 - 8b3ab47
- Phase-2A retrieval precision + runtime-cap hardening batch.
- Benchmark runner updates (benchmarking/run_amb_cortex.py):
  - Added strict single-run timeout guard with enforced 15-20 minute range.
  - Added explicit run-path timeout execution wrapper with hard process termination on cap breach.
  - Extended matrix controls for bounded case windows and global runtime caps.
- Adapter/client quality updates:
  - benchmarking/adapters/cortex_http_client.py now applies stronger query-intent/detail-aware reranking and suppresses low-signal assistant guidance snippets for personal factual recall.
  - benchmarking/adapters/cortex_amb_provider.py now extracts higher-signal fact snippets from conversation payloads and supports configurable full-doc vs fact-only storage.
- Daemon rate limiting update (daemon-rs/src/rate_limit.rs):
  - Split request limits for loopback vs non-loopback callers; kept auth-failure limiting unchanged.
- Tests/validation:
  - rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py sdks/python/tests/test_client.py -q -> 26 passed.
  - rtk python -m pytest benchmarking/adapters/tests/test_cortex_http_client.py benchmarking/adapters/tests/test_run_amb_cortex_matrix.py -q -> 21 passed.
  - rtk npm test in sdks/typescript -> 6 passed.
  - rtk cargo test --manifest-path daemon-rs/Cargo.toml rate_limit -- --nocapture -> 6 passed (353 filtered).
  - rtk cargo clippy --manifest-path daemon-rs/Cargo.toml --all-targets --all-features -- -D warnings currently reports existing non-batch failures in src/main.rs, src/state.rs, and src/embeddings.rs.
- Benchmark execution note:
  - Attempted strict-capped scored LongMemEval run (--max-runtime-seconds 1200) failed fast due missing answer/judge model API key in current shell.

## 2026-04-17 21:30 - Phase-2A fair-run preflight + real benchmark scoring
- Added strict fair-run preflight to benchmark runner (`benchmarking/run_amb_cortex.py`):
  - New persisted + printed `fair-run-preflight.json` for both `run` and `matrix` commands.
  - Fail-fast abort before execution when fairness policy is violated.
  - Matrix fairness now rejects default CLI shortcuts (`--query-id`, `--doc-limit`) in addition to case-level checks.
- Improved LongMemEval answer formatting for location precision:
  - Prompt shim now explicitly requires country/state qualifiers for location answers when present in context.
- Regression coverage updates:
  - `benchmarking/adapters/tests/test_run_amb_cortex_shims.py`: preflight pass/fail behavior for single-run path.
  - `benchmarking/adapters/tests/test_run_amb_cortex_matrix.py`: matrix default `query_id`/`doc_limit` fairness rejection and dry-run preflight artifact assertion.
- Validation:
  - `rtk python -m pytest benchmarking/adapters/tests/test_run_amb_cortex_shims.py sdks/python/tests/test_client.py -q` -> 43 passed.
  - `rtk python -m pytest benchmarking/adapters/tests/test_cortex_http_client.py benchmarking/adapters/tests/test_run_amb_cortex_matrix.py -q` -> 49 passed.
  - `rtk npm test` in `sdks/typescript` -> 6 passed.
- Strict/fair benchmark outcomes:
  - `benchmarking/runs/amb-run-20260417-210951`: `19/20`, preflight passed, gate failed on avg recall tokens (`234.15 > 213.20`).
  - `benchmarking/runs/amb-run-20260417-211640`: `20/20`, preflight passed, gate passed, avg recall tokens `200.6`, max `250`, total recall tokens `4012`.
  - `benchmarking/runs/amb-matrix-20260417-212238` (non-LongMem q5 slice, first 2 cases): preflight passed; cases failed strict quality gates (`locomo` 0/5, `lifebench` 1/5), establishing cross-dataset baseline.

## 2026-04-18 15:40 - Control Center startup timeout + dev lock-retry hardening
- Desktop app-managed daemon startup hardening (`desktop/cortex-control-center/src-tauri/src/main.rs`):
  - App-managed daemon spawn now forces loopback bind (`127.0.0.1`) for local ownership path, avoiding broad bind drift in desktop sessions.
- Control Center frontend reliability updates:
  - `src/App.jsx` now stages dashboard refresh (health/core first, secondary endpoints after core success) to avoid cold-start endpoint timeout storms.
  - Timeout-classification logic now treats IPC timeout errors as daemon-unavailable recovery conditions.
  - `src/api-client.js` now aligns IPC abort and transport budgets (`transport = max(500, abort - 500)`), and raises `/health` timeout to `12000ms` for startup warmup.
  - Updated `src/api-client.test.js` expectations to match timeout-budget behavior.
- Daemon startup contention reduction (`daemon-rs/src/main.rs`):
  - Added app-managed owner-sensitive deferral/staggering for startup heavy maintenance (indexing, embedding backfill, aging, crystallization) so initial API routes stay responsive.
  - Added regression coverage for control-center owner startup-delay behavior.
- Dev build binary-lock resilience (`desktop/cortex-control-center/scripts/ensure-daemon-dev-binary.mjs`):
  - Ensure script now rebuilds stale daemon binaries (mtime-based source staleness check).
  - Detects Windows locked-executable build failures, stops only the locked dev daemon process, and retries build once.
- Validation:
  - `rtk npm test -- api-client.test.js` -> 38 passed.
  - `rtk npm run web:build` -> passed.
  - `rtk cargo check --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` -> passed.
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml app_managed_startup_heavy_delay_only_applies_to_control_center_owner` -> passed.

## 2026-04-19 23:44 - cf43d05
- Control Center analytics copy cleanup for end users (`desktop/cortex-control-center/src/App.jsx`):
  - `Compounding return` footnote now explains only user-facing behavior:
    - `Rolling 30-day total tokens saved across boot compilations.`
    - Momentum variant keeps percent delta while removing internal implementation details.
  - `Throughput` footnote now reads:
    - `Rolling 7-day boot compilation count (~X/day)`.
  - Removed all user-visible references to internal event names and benchmark filtering language in these cards.
- Validation:
  - `rtk npm --prefix desktop/cortex-control-center test` -> 85 passed.
  - `rtk npm --prefix desktop/cortex-control-center run web:build` -> passed.

## 2026-04-19 23:52 - facfc5f
- Analytics clarity/legibility pass (`desktop/cortex-control-center/src/App.jsx`, `src/styles.css`):
  - Added a dedicated `Analytics metric legend` strip that defines each top KPI in plain language.
  - Updated economic-value assumption copy from `Assumes ...` to explicit `Assumption: ...`.
  - Center-aligned the analytics top-row KPI cards and normalized label/footnote alignment to reduce visual drift between cards.
  - Added responsive legend layouts for desktop/tablet/mobile breakpoints.
- Validation:
  - `rtk npm --prefix desktop/cortex-control-center test` -> 85 passed.
  - `rtk npm --prefix desktop/cortex-control-center run web:build` -> passed.

## 2026-04-20 00:22 - c92f91d
- Fixed an `Overview` panel blank-screen failure caused by brittle persisted UI state handling (`desktop/cortex-control-center/src/App.jsx`):
  - Added safe localStorage readers for UI bootstrap settings (`readLocalStorageValue`).
  - Added strict currency normalization (`normalizeCurrencyCode`) and migrated currency reads/writes to normalized values.
  - Hardened `Intl.NumberFormat` construction with fallback to USD when persisted currency is invalid.
  - Made `topbar-current` panel label rendering null-safe to avoid uppercase access on undefined labels.
  - Updated analytics currency select/label path to use normalized `safeCurrency`.
- Validation:
  - `rtk npm --prefix desktop/cortex-control-center test` -> 85 passed.
  - `rtk npm --prefix desktop/cortex-control-center run web:build` -> passed.
  - `expect`/`expect-cli` execution did not return actionable runtime traces in this environment (timeouts / non-zero exits), so browser-level reproduction artifacts were not produced from tooling.

## 2026-04-20 00:39 - ee864c6
- Removed a clippy `await_holding_lock` violation in recall latency-policy regression tests (`daemon-rs/src/handlers/recall.rs`):
  - dropped the test-only `std::sync::Mutex` environment guard that was held across async execution.
  - retained strict env var restore behavior after test execution.
  - keeps fail-closed recall latency-budget coverage while allowing `-D warnings` clippy gates to pass cleanly.
- Validation:
  - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml --all-targets --all-features -- -D warnings` -> passed.
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml --target-dir daemon-rs/target-codex-test execute_unified_recall_fail_closes_when_latency_budget_is_zero -- --nocapture` -> passed.

## 2026-04-20 01:05 - c67c4ad
- Reconciled canonical v0.5 closeout status wording in `docs/internal/CORTEX-UNIFIED-STATUS-PLAN.md`:
  - corrected the top-level “Current Reality” retrieval-default statement to match landed Phase-2A completion status (`all-MiniLM-L12-v2` default, guarded sqlite-vec `primary` routing, explicit embedding migration closeout controls).
  - refreshed owner-doc timestamp to reflect the canonical-state sync pass.
- Validation:
  - Docs-only consistency commit (no runtime code path changes).

## 2026-04-21 22:12 - pending local release-hardening stack (pre-push)
- Security dependency + runtime hardening:
  - daemon/service install path now sanitizes `%USERNAME%` before composing `sc.exe` command string (`daemon-rs/src/service.rs`) and adds regression tests for metacharacter rejection.
  - Rust lockfiles updated to `rustls-webpki 0.103.12` for daemon and desktop tauri.
  - Control Center npm tree forced to patched `hono` via package override; `npm audit` now returns zero vulnerabilities.
  - fixed clippy borrow warning in MCP auth-token reader (`daemon-rs/src/mcp_proxy.rs`).
- Benchmark/public artifact policy:
  - raw benchmark score artifacts are now explicitly allowed in git for `benchmarking/results/*.{json,md,csv,tsv}`.
  - `.ignore` aligned so tooling no longer hides `benchmarking/results/`.
  - benchmark smoke pass recorded at `benchmarking/runs/amb-smoke-20260421-220511`.
- Developer-coupling/path scrub:
  - removed hardcoded developer-home paths and personal-name markers from runtime-adjacent tests/mocks/docs/UI strings.
  - updated tauri updater endpoint owner segment to neutral project namespace.
  - generalized clean-install path checks and dynamicized build-bloat script repo-root default.
- Security process hardening:
  - added `.github/workflows/security-hygiene.yml` (gitleaks + path leak + `.env` + benchmark visibility policy checks).
- Bloat cleanup:
  - removed duplicate generated schema file `desktop/cortex-control-center/src-tauri/gen/schemas/windows-schema.json` (unreferenced duplicate of desktop schema content).
- Validation snapshot:
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml recall::tests:: -- --nocapture` -> pass
  - `rtk cargo test --manifest-path daemon-rs/Cargo.toml handlers::mcp::tests:: -- --nocapture` -> pass
  - `npm test` in `desktop/cortex-control-center` -> pass
  - `rtk cargo test --manifest-path desktop/cortex-control-center/src-tauri/Cargo.toml` -> pass
  - `rtk cargo clippy --manifest-path daemon-rs/Cargo.toml -- -D warnings` -> pass
  - `bash scripts/clean_install_smoke.sh` -> `GATE: PASSED`
