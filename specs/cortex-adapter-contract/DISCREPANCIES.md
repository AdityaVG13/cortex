# Cortex Adapter Contract Discrepancies

No accepted divergences are currently defined for `specs/cortex-adapter-contract.yaml`.

When an intentional divergence is accepted, add a `DISC-NNN` entry here and set the affected scenario's `coverageStatus` to `xfail` with the same `discrepancyId`.
